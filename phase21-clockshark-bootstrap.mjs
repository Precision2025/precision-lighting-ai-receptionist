import fs from "node:fs";

const serverPath = new URL(
  "./server.js",
  import.meta.url
);
let server = fs.readFileSync(
  serverPath,
  "utf8"
);

const MARKER =
  "JOSHUA_PHASE21_CLOCKSHARK_LIVE_OPERATIONS_V1";

if (!server.includes(MARKER)) {
  const helperAnchor =
    "function controlAuthorized(request) {";

  if (!server.includes(helperAnchor)) {
    throw new Error(
      "Could not locate Joshua's control authorization anchor for Phase 21."
    );
  }

  const helpers = "/* JOSHUA_PHASE21_CLOCKSHARK_LIVE_OPERATIONS_V1 */\nconst PHASE21_CLOCKSHARK_TIME_ZONE =\n  process.env.BUSINESS_TIME_ZONE ||\n  \"America/Chicago\";\n\nconst PHASE21_CLOCKSHARK_SYNC_MINUTES = Math.max(\n  1,\n  Number(\n    process.env.CLOCKSHARK_SYNC_MINUTES ||\n    5\n  )\n);\n\nconst PHASE21_CLOCKSHARK_STALE_HOURS = Math.max(\n  1,\n  Number(\n    process.env.CLOCKSHARK_STALE_HOURS ||\n    12\n  )\n);\n\nfunction phase21ClockSharkNow() {\n  return new Date().toISOString();\n}\n\nfunction phase21ClockSharkId(prefix = \"clockshark\") {\n  return [\n    prefix,\n    Date.now(),\n    Math.random().toString(36).slice(2, 9)\n  ].join(\"-\");\n}\n\nfunction phase21ClockSharkText(value = \"\") {\n  return String(value ?? \"\").trim();\n}\n\nfunction phase21ClockSharkNumber(value, fallback = 0) {\n  const text = String(value ?? \"\")\n    .replace(/,/g, \"\")\n    .trim();\n\n  if (!text) return fallback;\n\n  const number = Number(text);\n  return Number.isFinite(number)\n    ? number\n    : fallback;\n}\n\nfunction phase21ClockSharkDate(value = \"\") {\n  if (!value) return \"\";\n\n  const date = new Date(value);\n  return Number.isFinite(date.getTime())\n    ? date.toISOString()\n    : \"\";\n}\n\nfunction phase21ClockSharkFirst(\n  payload = {},\n  keys = []\n) {\n  for (const key of keys) {\n    const value = payload?.[key];\n\n    if (\n      value !== undefined &&\n      value !== null &&\n      String(value).trim() !== \"\"\n    ) {\n      return value;\n    }\n  }\n\n  return \"\";\n}\n\nfunction phase21ClockSharkArray(value) {\n  if (Array.isArray(value)) return value;\n  if (value === undefined || value === null || value === \"\") {\n    return [];\n  }\n  return [value];\n}\n\nfunction phase21ClockSharkUnique(values = []) {\n  const output = [];\n  const seen = new Set();\n\n  for (const value of values) {\n    const text = phase21ClockSharkText(value);\n    if (!text) continue;\n\n    const key = text.toLowerCase();\n    if (seen.has(key)) continue;\n\n    seen.add(key);\n    output.push(text);\n  }\n\n  return output;\n}\n\nfunction phase21ClockSharkLocalDate(value = new Date()) {\n  const date =\n    value instanceof Date\n      ? value\n      : new Date(value);\n\n  if (!Number.isFinite(date.getTime())) {\n    return \"\";\n  }\n\n  return new Intl.DateTimeFormat(\n    \"en-CA\",\n    {\n      timeZone:\n        PHASE21_CLOCKSHARK_TIME_ZONE,\n      year: \"numeric\",\n      month: \"2-digit\",\n      day: \"2-digit\"\n    }\n  ).format(date);\n}\n\nfunction phase21ClockSharkWeekStart() {\n  const now = new Date();\n  const localDate =\n    phase21ClockSharkLocalDate(now);\n  const date = new Date(\n    `${localDate}T12:00:00Z`\n  );\n  const day = date.getUTCDay();\n  const offset = day === 0 ? -6 : 1 - day;\n  date.setUTCDate(\n    date.getUTCDate() + offset\n  );\n  return date\n    .toISOString()\n    .slice(0, 10);\n}\n\nfunction phase21ClockSharkCanonicalType(\n  raw = \"\"\n) {\n  const value =\n    phase21ClockSharkText(raw)\n      .toLowerCase()\n      .replace(/[\\s-]+/g, \"_\");\n\n  if (\n    /clock_?in|new_?clock_?in|started_?shift/.test(\n      value\n    )\n  ) {\n    return \"clock_in\";\n  }\n\n  if (\n    /clock_?out|new_?clock_?out|ended_?shift|completed_?shift/.test(\n      value\n    )\n  ) {\n    return \"clock_out\";\n  }\n\n  if (/new_?job|job_?added|job_?created/.test(value)) {\n    return \"job_added\";\n  }\n\n  if (\n    /new_?schedule|schedule_?added|schedule_?created/.test(\n      value\n    )\n  ) {\n    return \"schedule_added\";\n  }\n\n  if (\n    /notification|alert/.test(value)\n  ) {\n    return \"notification\";\n  }\n\n  if (\n    /snapshot|sync|report|csv/.test(value)\n  ) {\n    return \"snapshot\";\n  }\n\n  return value || \"snapshot\";\n}\n\nfunction phase21ClockSharkTracking(\n  ...values\n) {\n  for (const value of values) {\n    const text =\n      phase21ClockSharkText(value);\n\n    if (!text) continue;\n\n    const labelled = text.match(\n      /(?:tracking|work\\s*order|wo|job)\\s*(?:number|no\\.?|#)?\\s*[:#-]?\\s*(\\d{3,14})/i\n    );\n\n    if (labelled) {\n      return labelled[1];\n    }\n\n    const leading = text.match(\n      /^\\s*#?(\\d{3,14})(?:\\s*[|—–-]|$)/\n    );\n\n    if (leading) {\n      return leading[1];\n    }\n\n    if (/^\\d{3,14}$/.test(text)) {\n      return text;\n    }\n\n    const any = text.match(\n      /\\b(\\d{3,14})\\b/\n    );\n\n    if (any) {\n      return any[1];\n    }\n  }\n\n  return \"\";\n}\n\nfunction phase21ClockSharkEmployee(\n  payload = {}\n) {\n  const firstName =\n    phase21ClockSharkText(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"employeeFirstName\",\n          \"employee_first_name\",\n          \"firstName\",\n          \"first_name\"\n        ]\n      )\n    );\n\n  const lastName =\n    phase21ClockSharkText(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"employeeLastName\",\n          \"employee_last_name\",\n          \"lastName\",\n          \"last_name\"\n        ]\n      )\n    );\n\n  const nested =\n    payload.employee &&\n    typeof payload.employee === \"object\"\n      ? payload.employee\n      : {};\n\n  const nestedFirst =\n    phase21ClockSharkText(\n      nested.firstName ||\n      nested.first_name\n    );\n\n  const nestedLast =\n    phase21ClockSharkText(\n      nested.lastName ||\n      nested.last_name\n    );\n\n  const name =\n    phase21ClockSharkText(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"employeeName\",\n          \"employee_name\",\n          \"technicianName\",\n          \"technician_name\",\n          \"displayName\",\n          \"fullName\",\n          \"name\"\n        ]\n      ) ||\n      nested.name ||\n      nested.displayName ||\n      [\n        firstName || nestedFirst,\n        lastName || nestedLast\n      ].filter(Boolean).join(\" \")\n    );\n\n  const id =\n    phase21ClockSharkText(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"employeeId\",\n          \"employee_id\",\n          \"technicianId\",\n          \"technician_id\",\n          \"userId\",\n          \"user_id\"\n        ]\n      ) ||\n      nested.id ||\n      nested.employeeId ||\n      name\n    );\n\n  return {\n    id,\n    name,\n    firstName:\n      firstName ||\n      nestedFirst,\n    lastName:\n      lastName ||\n      nestedLast,\n    phone:\n      phase21ClockSharkText(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"employeePhone\",\n            \"employee_phone\",\n            \"phone\",\n            \"mobile\",\n            \"mobilePhone\"\n          ]\n        ) ||\n        nested.phone ||\n        nested.mobile\n      ),\n    email:\n      phase21ClockSharkText(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"employeeEmail\",\n            \"employee_email\",\n            \"email\"\n          ]\n        ) ||\n        nested.email\n      )\n  };\n}\n\nfunction phase21ClockSharkJob(\n  payload = {}\n) {\n  const nested =\n    payload.job &&\n    typeof payload.job === \"object\"\n      ? payload.job\n      : {};\n\n  const number =\n    phase21ClockSharkText(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"jobNumber\",\n          \"job_number\",\n          \"workOrderNumber\",\n          \"work_order_number\",\n          \"trackingNumber\",\n          \"tracking_number\"\n        ]\n      ) ||\n      nested.number ||\n      nested.jobNumber\n    );\n\n  const name =\n    phase21ClockSharkText(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"jobName\",\n          \"job_name\",\n          \"customerJob\",\n          \"customer_job\",\n          \"projectName\",\n          \"project_name\"\n        ]\n      ) ||\n      nested.name ||\n      nested.jobName\n    );\n\n  const trackingNumber =\n    phase21ClockSharkTracking(\n      number,\n      name,\n      payload.description,\n      nested.description\n    );\n\n  return {\n    id:\n      phase21ClockSharkText(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"jobId\",\n            \"job_id\",\n            \"projectId\",\n            \"project_id\"\n          ]\n        ) ||\n        nested.id ||\n        trackingNumber ||\n        name\n      ),\n    number,\n    trackingNumber,\n    name,\n    customer:\n      phase21ClockSharkText(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"customerName\",\n            \"customer_name\",\n            \"customer\"\n          ]\n        ) ||\n        nested.customerName ||\n        nested.customer\n      ),\n    description:\n      phase21ClockSharkText(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"jobDescription\",\n            \"job_description\",\n            \"description\",\n            \"scope\"\n          ]\n        ) ||\n        nested.description\n      ),\n    address:\n      phase21ClockSharkText(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"jobAddress\",\n            \"job_address\",\n            \"address\",\n            \"street1\"\n          ]\n        ) ||\n        nested.address\n      ),\n    city:\n      phase21ClockSharkText(\n        payload.city ||\n        nested.city\n      ),\n    state:\n      phase21ClockSharkText(\n        payload.state ||\n        payload.stateProvince ||\n        nested.state\n      ),\n    postalCode:\n      phase21ClockSharkText(\n        payload.postalCode ||\n        payload.postal_code ||\n        payload.zip ||\n        nested.postalCode\n      ),\n    stage:\n      phase21ClockSharkText(\n        payload.jobStage ||\n        payload.job_stage ||\n        payload.stage ||\n        nested.stage\n      ),\n    active:\n      payload.active !== false &&\n      payload.isActive !== false &&\n      nested.active !== false\n  };\n}\n\nfunction phase21ClockSharkHours(\n  payload = {},\n  clockInAt = \"\",\n  clockOutAt = \"\"\n) {\n  let totalHours =\n    phase21ClockSharkNumber(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"totalHours\",\n          \"total_hours\",\n          \"durationHours\",\n          \"duration_hours\",\n          \"hours\",\n          \"workHours\",\n          \"work_hours\"\n        ]\n      ),\n      NaN\n    );\n\n  if (!Number.isFinite(totalHours)) {\n    const totalMinutes =\n      phase21ClockSharkNumber(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"totalMinutes\",\n            \"total_minutes\",\n            \"durationMinutes\",\n            \"duration_minutes\",\n            \"minutes\"\n          ]\n        ),\n        NaN\n      );\n\n    if (Number.isFinite(totalMinutes)) {\n      totalHours =\n        totalMinutes / 60;\n    }\n  }\n\n  const breakMinutes =\n    phase21ClockSharkNumber(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"breakMinutes\",\n          \"break_minutes\",\n          \"breakDurationMinutes\",\n          \"break_duration_minutes\"\n        ]\n      ),\n      0\n    );\n\n  if (\n    !Number.isFinite(totalHours) &&\n    clockInAt &&\n    clockOutAt\n  ) {\n    totalHours = Math.max(\n      0,\n      (\n        new Date(clockOutAt).getTime() -\n        new Date(clockInAt).getTime() -\n        breakMinutes * 60000\n      ) /\n      3600000\n    );\n  }\n\n  totalHours =\n    Number.isFinite(totalHours)\n      ? totalHours\n      : 0;\n\n  const overtimeHours =\n    phase21ClockSharkNumber(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"overtimeHours\",\n          \"overtime_hours\",\n          \"otHours\",\n          \"ot_hours\"\n        ]\n      ),\n      0\n    );\n\n  let regularHours =\n    phase21ClockSharkNumber(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"regularHours\",\n          \"regular_hours\",\n          \"straightTimeHours\",\n          \"straight_time_hours\"\n        ]\n      ),\n      NaN\n    );\n\n  if (!Number.isFinite(regularHours)) {\n    regularHours = Math.max(\n      0,\n      totalHours - overtimeHours\n    );\n  }\n\n  return {\n    totalHours:\n      Number(totalHours.toFixed(4)),\n    regularHours:\n      Number(regularHours.toFixed(4)),\n    overtimeHours:\n      Number(overtimeHours.toFixed(4)),\n    breakMinutes:\n      Number(breakMinutes.toFixed(2))\n  };\n}\n\nfunction phase21ClockSharkShift(\n  payload = {},\n  eventType = \"snapshot\"\n) {\n  const employee =\n    phase21ClockSharkEmployee(payload);\n  const job =\n    phase21ClockSharkJob(payload);\n\n  const clockInAt =\n    phase21ClockSharkDate(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"clockInAt\",\n          \"clock_in_at\",\n          \"clockIn\",\n          \"clock_in\",\n          \"startTime\",\n          \"start_time\",\n          \"start\",\n          \"dateTimeIn\",\n          \"date_time_in\"\n        ]\n      )\n    );\n\n  const clockOutAt =\n    phase21ClockSharkDate(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"clockOutAt\",\n          \"clock_out_at\",\n          \"clockOut\",\n          \"clock_out\",\n          \"endTime\",\n          \"end_time\",\n          \"end\",\n          \"dateTimeOut\",\n          \"date_time_out\"\n        ]\n      )\n    );\n\n  const hours =\n    phase21ClockSharkHours(\n      payload,\n      clockInAt,\n      clockOutAt\n    );\n\n  const explicitId =\n    phase21ClockSharkText(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"shiftId\",\n          \"shift_id\",\n          \"timeEntryId\",\n          \"time_entry_id\",\n          \"clockId\",\n          \"clock_id\",\n          \"id\"\n        ]\n      )\n    );\n\n  const attachments =\n    phase21ClockSharkUnique(\n      [\n        ...phase21ClockSharkArray(\n          payload.attachments\n        ),\n        ...phase21ClockSharkArray(\n          payload.attachmentUrls\n        ),\n        ...phase21ClockSharkArray(\n          payload.attachment_urls\n        ),\n        payload.attachmentUrl,\n        payload.attachment_url,\n        payload.photoUrl,\n        payload.photo_url\n      ]\n    );\n\n  return {\n    id: explicitId,\n    eventType,\n    employeeId: employee.id,\n    employeeName: employee.name,\n    employeePhone: employee.phone,\n    employeeEmail: employee.email,\n    jobId: job.id,\n    jobNumber: job.number,\n    trackingNumber:\n      job.trackingNumber,\n    jobName: job.name,\n    customer: job.customer,\n    task:\n      phase21ClockSharkText(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"taskName\",\n            \"task_name\",\n            \"task\",\n            \"taskCode\",\n            \"task_code\"\n          ]\n        )\n      ),\n    clockInAt,\n    clockOutAt,\n    status:\n      clockOutAt ||\n      eventType === \"clock_out\"\n        ? \"closed\"\n        : \"open\",\n    notes:\n      phase21ClockSharkText(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"notes\",\n            \"note\",\n            \"shiftNotes\",\n            \"shift_notes\",\n            \"clockOutNotes\",\n            \"clock_out_notes\",\n            \"description\"\n          ]\n        )\n      ),\n    attachments,\n    ...hours,\n    source:\n      phase21ClockSharkText(\n        payload.source ||\n        \"ClockShark\"\n      ),\n    rawReceivedAt:\n      phase21ClockSharkNow()\n  };\n}\n\nfunction phase21ClockSharkSchedule(\n  payload = {}\n) {\n  const employee =\n    phase21ClockSharkEmployee(payload);\n  const job =\n    phase21ClockSharkJob(payload);\n\n  return {\n    id:\n      phase21ClockSharkText(\n        phase21ClockSharkFirst(\n          payload,\n          [\n            \"scheduleId\",\n            \"schedule_id\",\n            \"shiftId\",\n            \"shift_id\",\n            \"id\"\n          ]\n        ) ||\n        [\n          employee.id ||\n          employee.name,\n          job.id ||\n          job.trackingNumber ||\n          job.name,\n          payload.startTime ||\n          payload.start_time ||\n          payload.date\n        ].join(\"|\")\n      ),\n    employeeId: employee.id,\n    employeeName: employee.name,\n    jobId: job.id,\n    jobNumber: job.number,\n    trackingNumber:\n      job.trackingNumber,\n    jobName: job.name,\n    task:\n      phase21ClockSharkText(\n        payload.taskName ||\n        payload.task_name ||\n        payload.task\n      ),\n    startAt:\n      phase21ClockSharkDate(\n        payload.startAt ||\n        payload.start_at ||\n        payload.startTime ||\n        payload.start_time ||\n        payload.start\n      ),\n    endAt:\n      phase21ClockSharkDate(\n        payload.endAt ||\n        payload.end_at ||\n        payload.endTime ||\n        payload.end_time ||\n        payload.end\n      ),\n    notes:\n      phase21ClockSharkText(\n        payload.notes ||\n        payload.note ||\n        payload.description\n      ),\n    receivedAt:\n      phase21ClockSharkNow()\n  };\n}\n\nfunction phase21ClockSharkEnsureData(data) {\n  data.clockShark =\n    data.clockShark &&\n    typeof data.clockShark === \"object\"\n      ? data.clockShark\n      : {};\n\n  const state = data.clockShark;\n\n  state.version = 1;\n  state.employees =\n    state.employees &&\n    typeof state.employees === \"object\"\n      ? state.employees\n      : {};\n  state.jobs =\n    state.jobs &&\n    typeof state.jobs === \"object\"\n      ? state.jobs\n      : {};\n  state.shifts =\n    state.shifts &&\n    typeof state.shifts === \"object\"\n      ? state.shifts\n      : {};\n  state.schedules =\n    state.schedules &&\n    typeof state.schedules === \"object\"\n      ? state.schedules\n      : {};\n  state.notifications =\n    Array.isArray(state.notifications)\n      ? state.notifications\n      : [];\n  state.events =\n    Array.isArray(state.events)\n      ? state.events\n      : [];\n  state.processedKeys =\n    Array.isArray(state.processedKeys)\n      ? state.processedKeys\n      : [];\n  state.sync =\n    state.sync &&\n    typeof state.sync === \"object\"\n      ? state.sync\n      : {};\n  state.imports =\n    Array.isArray(state.imports)\n      ? state.imports\n      : [];\n\n  state.sync.inboundConfigured =\n    Boolean(\n      phase21ClockSharkText(\n        process.env.CLOCKSHARK_INBOUND_SECRET\n      )\n    );\n  state.sync.feedConfigured =\n    Boolean(\n      phase21ClockSharkText(\n        process.env.CLOCKSHARK_OPERATIONS_FEED_URL\n      )\n    );\n  state.sync.rosterConfigured =\n    Boolean(\n      phase21ClockSharkText(\n        process.env.CLOCKSHARK_TECHNICIANS_WEBHOOK_URL\n      )\n    );\n\n  return state;\n}\n\nfunction phase21ClockSharkEventKey(\n  eventType,\n  payload\n) {\n  const explicit =\n    phase21ClockSharkText(\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"eventId\",\n          \"event_id\",\n          \"zapId\",\n          \"zap_id\",\n          \"shiftId\",\n          \"shift_id\",\n          \"timeEntryId\",\n          \"time_entry_id\",\n          \"id\"\n        ]\n      )\n    );\n\n  if (explicit) {\n    return [\n      eventType,\n      explicit\n    ].join(\"|\");\n  }\n\n  const important = {\n    eventType,\n    employee:\n      phase21ClockSharkEmployee(payload),\n    job:\n      phase21ClockSharkJob(payload),\n    clockIn:\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"clockInAt\",\n          \"clock_in_at\",\n          \"clockIn\",\n          \"clock_in\",\n          \"startTime\",\n          \"start_time\",\n          \"start\"\n        ]\n      ),\n    clockOut:\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"clockOutAt\",\n          \"clock_out_at\",\n          \"clockOut\",\n          \"clock_out\",\n          \"endTime\",\n          \"end_time\",\n          \"end\"\n        ]\n      ),\n    totalHours:\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"totalHours\",\n          \"total_hours\",\n          \"durationHours\",\n          \"duration_hours\"\n        ]\n      ),\n    notes:\n      phase21ClockSharkFirst(\n        payload,\n        [\n          \"notes\",\n          \"note\",\n          \"shiftNotes\",\n          \"shift_notes\"\n        ]\n      )\n  };\n\n  return [\n    eventType,\n    crypto\n      .createHash(\"sha256\")\n      .update(\n        JSON.stringify(important)\n      )\n      .digest(\"hex\")\n  ].join(\"|\");\n}\n\nfunction phase21ClockSharkAddEvent(\n  data,\n  state,\n  eventType,\n  payload,\n  result = {}\n) {\n  const event = {\n    id:\n      phase21ClockSharkId(\n        \"clockshark-event\"\n      ),\n    eventType,\n    createdAt:\n      phase21ClockSharkNow(),\n    employeeName:\n      result.employeeName ||\n      phase21ClockSharkEmployee(\n        payload\n      ).name,\n    trackingNumber:\n      result.trackingNumber ||\n      phase21ClockSharkJob(\n        payload\n      ).trackingNumber,\n    jobName:\n      result.jobName ||\n      phase21ClockSharkJob(\n        payload\n      ).name,\n    result:\n      result.result ||\n      \"processed\",\n    source:\n      phase21ClockSharkText(\n        payload.source ||\n        \"ClockShark\"\n      )\n  };\n\n  state.events.unshift(event);\n  state.events =\n    state.events.slice(0, 2000);\n\n  data.events =\n    Array.isArray(data.events)\n      ? data.events\n      : [];\n\n  data.events.unshift({\n    ...event,\n    type:\n      `clockshark_${eventType}`,\n    level:\n      result.level ||\n      \"info\",\n    message:\n      result.message ||\n      [\n        \"ClockShark\",\n        eventType.replaceAll(\"_\", \" \"),\n        event.employeeName,\n        event.trackingNumber\n          ? `#${event.trackingNumber}`\n          : event.jobName\n      ].filter(Boolean).join(\" — \")\n  });\n  data.events =\n    data.events.slice(0, 500);\n\n  return event;\n}\n\nfunction phase21ClockSharkTaskExists(\n  data,\n  workflowType,\n  trackingNumber,\n  identity = \"\"\n) {\n  return (data.tasks || []).some(task => {\n    const sameTask = Boolean(\n      String(task.workflowType || \"\") ===\n        String(workflowType || \"\") &&\n      String(task.trackingNumber || \"\") ===\n        String(trackingNumber || \"\") &&\n      (\n        !identity ||\n        String(task.clockSharkIdentity || \"\") ===\n          String(identity)\n      )\n    );\n\n    if (!sameTask) return false;\n\n    const closed = [\"closed\", \"completed\"].includes(\n      String(task.status || \"\").toLowerCase()\n    );\n\n    if (!closed) return true;\n\n    // Resolved means resolved. A dashboard button must not become\n    // permission for the same automatic alert to recreate itself.\n    const humanResolved = Boolean(\n      task.phase25HumanResolved === true ||\n      (\n        (task.closedAt || task.completedAt || task.completedBy) &&\n        !task.closedReason &&\n        !task.autoClosedReason\n      )\n    );\n\n    return humanResolved;\n  });\n}\n\nfunction phase21ClockSharkCreateTask(\n  data,\n  {\n    workflowType,\n    title,\n    trackingNumber = \"\",\n    assignedTo = \"Ariana\",\n    priority = \"urgent\",\n    notes = \"\",\n    identity = \"\",\n    actionLabel = \"Mark Complete\",\n    dueMinutes = 120\n  }\n) {\n  data.tasks =\n    Array.isArray(data.tasks)\n      ? data.tasks\n      : [];\n\n  if (\n    phase21ClockSharkTaskExists(\n      data,\n      workflowType,\n      trackingNumber,\n      identity\n    )\n  ) {\n    return null;\n  }\n\n  const now = new Date();\n\n  const task = {\n    id:\n      phase21ClockSharkId(\"task\"),\n    createdAt:\n      now.toISOString(),\n    status: \"open\",\n    title,\n    trackingNumber:\n      String(trackingNumber || \"\"),\n    assignedTo,\n    priority,\n    workflowType,\n    notes,\n    actionLabel,\n    source:\n      \"ClockShark Live Operations\",\n    clockSharkIdentity:\n      String(identity || \"\"),\n    dueAt:\n      new Date(\n        now.getTime() +\n        Number(dueMinutes || 120) *\n          60000\n      ).toISOString()\n  };\n\n  data.tasks.unshift(task);\n  data.tasks =\n    data.tasks.slice(0, 500);\n\n  return task;\n}\n\nfunction phase21ClockSharkUpsertEmployee(\n  data,\n  state,\n  employee,\n  updates = {}\n) {\n  if (!employee.id && !employee.name) {\n    return null;\n  }\n\n  const key =\n    employee.id ||\n    employee.name.toLowerCase();\n\n  const current =\n    state.employees[key] || {};\n\n  state.employees[key] = {\n    ...current,\n    ...employee,\n    ...updates,\n    id:\n      employee.id ||\n      current.id ||\n      key,\n    name:\n      employee.name ||\n      current.name ||\n      key,\n    updatedAt:\n      phase21ClockSharkNow()\n  };\n\n  if (employee.name) {\n    data.technicians =\n      data.technicians &&\n      typeof data.technicians === \"object\"\n        ? data.technicians\n        : {};\n\n    const currentTech =\n      data.technicians[\n        employee.name\n      ] || {\n        name:\n          employee.name,\n        createdAt:\n          phase21ClockSharkNow(),\n        status: \"available\",\n        skills: []\n      };\n\n    data.technicians[\n      employee.name\n    ] = {\n      ...currentTech,\n      name:\n        employee.name,\n      phone:\n        employee.phone ||\n        currentTech.phone ||\n        \"\",\n      email:\n        employee.email ||\n        currentTech.email ||\n        \"\",\n      clockSharkId:\n        employee.id ||\n        currentTech.clockSharkId ||\n        \"\",\n      ...updates,\n      updatedAt:\n        phase21ClockSharkNow()\n    };\n  }\n\n  return state.employees[key];\n}\n\nfunction phase21ClockSharkUpsertJob(\n  state,\n  job\n) {\n  if (\n    !job.id &&\n    !job.trackingNumber &&\n    !job.name\n  ) {\n    return null;\n  }\n\n  const key =\n    job.id ||\n    job.trackingNumber ||\n    job.name.toLowerCase();\n\n  state.jobs[key] = {\n    ...(state.jobs[key] || {}),\n    ...job,\n    id:\n      job.id ||\n      state.jobs[key]?.id ||\n      key,\n    updatedAt:\n      phase21ClockSharkNow()\n  };\n\n  return state.jobs[key];\n}\n\nfunction phase21ClockSharkFindOpenShift(\n  state,\n  shift\n) {\n  const shifts =\n    Object.values(state.shifts);\n\n  const employeeKey =\n    phase21ClockSharkText(\n      shift.employeeId ||\n      shift.employeeName\n    ).toLowerCase();\n\n  const jobKey =\n    phase21ClockSharkText(\n      shift.jobId ||\n      shift.trackingNumber ||\n      shift.jobNumber ||\n      shift.jobName\n    ).toLowerCase();\n\n  return shifts\n    .filter(item =>\n      item.status === \"open\" &&\n      (\n        !employeeKey ||\n        [\n          item.employeeId,\n          item.employeeName\n        ].some(value =>\n          phase21ClockSharkText(value)\n            .toLowerCase() ===\n          employeeKey\n        )\n      ) &&\n      (\n        !jobKey ||\n        [\n          item.jobId,\n          item.trackingNumber,\n          item.jobNumber,\n          item.jobName\n        ].some(value =>\n          phase21ClockSharkText(value)\n            .toLowerCase() ===\n          jobKey\n        )\n      )\n    )\n    .sort(\n      (a, b) =>\n        new Date(\n          b.clockInAt ||\n          b.createdAt ||\n          0\n        ).getTime() -\n        new Date(\n          a.clockInAt ||\n          a.createdAt ||\n          0\n        ).getTime()\n    )[0] || null;\n}\n\nfunction phase21ClockSharkShiftKey(\n  shift\n) {\n  if (shift.id) {\n    return String(shift.id);\n  }\n\n  return crypto\n    .createHash(\"sha256\")\n    .update(\n      [\n        shift.employeeId ||\n        shift.employeeName,\n        shift.jobId ||\n        shift.trackingNumber ||\n        shift.jobName,\n        shift.clockInAt ||\n        \"open\"\n      ].join(\"|\")\n    )\n    .digest(\"hex\");\n}\n\nfunction phase21ClockSharkRecalculateWorkOrder(\n  data,\n  state,\n  trackingNumber\n) {\n  const tracking =\n    phase21ClockSharkText(\n      trackingNumber\n    );\n\n  if (!tracking) return null;\n\n  data.workOrders =\n    data.workOrders &&\n    typeof data.workOrders === \"object\"\n      ? data.workOrders\n      : {};\n\n  const workOrderKey =\n    Object.keys(\n      data.workOrders\n    ).find(key =>\n      String(key) === tracking ||\n      String(\n        data.workOrders[key]\n          ?.trackingNumber ||\n        data.workOrders[key]\n          ?.workOrderNumber ||\n        \"\"\n      ) === tracking\n    );\n\n  if (!workOrderKey) {\n    return null;\n  }\n\n  const shifts =\n    Object.values(state.shifts)\n      .filter(shift =>\n        String(\n          shift.trackingNumber || \"\"\n        ) === tracking\n      );\n\n  const closed =\n    shifts.filter(shift =>\n      shift.status === \"closed\"\n    );\n\n  const open =\n    shifts.filter(shift =>\n      shift.status === \"open\"\n    );\n\n  const sum = key =>\n    closed.reduce(\n      (total, shift) =>\n        total +\n        phase21ClockSharkNumber(\n          shift[key],\n          0\n        ),\n      0\n    );\n\n  const notes =\n    phase21ClockSharkUnique(\n      closed.map(shift =>\n        shift.notes\n      )\n    ).slice(-100);\n\n  const employees =\n    phase21ClockSharkUnique(\n      shifts.map(shift =>\n        shift.employeeName\n      )\n    );\n\n  const current =\n    data.workOrders[\n      workOrderKey\n    ];\n\n  data.workOrders[\n    workOrderKey\n  ] = {\n    ...current,\n    trackingNumber:\n      current.trackingNumber ||\n      tracking,\n    clockSharkLaborHours:\n      Number(\n        sum(\"totalHours\")\n          .toFixed(4)\n      ),\n    clockSharkRegularHours:\n      Number(\n        sum(\"regularHours\")\n          .toFixed(4)\n      ),\n    clockSharkOvertimeHours:\n      Number(\n        sum(\"overtimeHours\")\n          .toFixed(4)\n      ),\n    clockSharkEmployees:\n      employees,\n    clockSharkNotes:\n      notes,\n    clockSharkOpenShiftCount:\n      open.length,\n    clockSharkTimeEntryCount:\n      shifts.length,\n    clockSharkCurrentlyClockedIn:\n      open.length > 0,\n    clockSharkLastClockInAt:\n      shifts\n        .map(item =>\n          item.clockInAt\n        )\n        .filter(Boolean)\n        .sort()\n        .at(-1) || \"\",\n    clockSharkLastClockOutAt:\n      closed\n        .map(item =>\n          item.clockOutAt\n        )\n        .filter(Boolean)\n        .sort()\n        .at(-1) || \"\",\n    clockSharkLastSyncAt:\n      phase21ClockSharkNow(),\n    updatedAt:\n      phase21ClockSharkNow()\n  };\n\n  return data.workOrders[\n    workOrderKey\n  ];\n}\n\nfunction phase21ClockSharkApplyShift(\n  data,\n  state,\n  payload,\n  eventType\n) {\n  let shift =\n    phase21ClockSharkShift(\n      payload,\n      eventType\n    );\n\n  const employee =\n    phase21ClockSharkEmployee(\n      payload\n    );\n  const job =\n    phase21ClockSharkJob(\n      payload\n    );\n\n  phase21ClockSharkUpsertEmployee(\n    data,\n    state,\n    employee\n  );\n  phase21ClockSharkUpsertJob(\n    state,\n    job\n  );\n\n  let existing = null;\n\n  if (shift.id) {\n    existing =\n      state.shifts[\n        shift.id\n      ] || null;\n  }\n\n  if (\n    !existing &&\n    eventType === \"clock_out\"\n  ) {\n    existing =\n      phase21ClockSharkFindOpenShift(\n        state,\n        shift\n      );\n  }\n\n  if (existing) {\n    shift = {\n      ...existing,\n      ...shift,\n      id:\n        existing.id ||\n        shift.id,\n      clockInAt:\n        shift.clockInAt ||\n        existing.clockInAt,\n      notes:\n        shift.notes ||\n        existing.notes,\n      attachments:\n        phase21ClockSharkUnique([\n          ...(existing.attachments || []),\n          ...(shift.attachments || [])\n        ])\n    };\n\n    const recomputed =\n      phase21ClockSharkHours(\n        shift,\n        shift.clockInAt,\n        shift.clockOutAt\n      );\n\n    shift = {\n      ...shift,\n      ...recomputed,\n      status:\n        shift.clockOutAt ||\n        eventType === \"clock_out\"\n          ? \"closed\"\n          : \"open\"\n    };\n  }\n\n  const key =\n    existing\n      ? Object.keys(\n          state.shifts\n        ).find(candidate =>\n          state.shifts[candidate] ===\n          existing\n        ) ||\n        phase21ClockSharkShiftKey(\n          shift\n        )\n      : phase21ClockSharkShiftKey(\n          shift\n        );\n\n  shift.id =\n    shift.id ||\n    key;\n  shift.createdAt =\n    existing?.createdAt ||\n    phase21ClockSharkNow();\n  shift.updatedAt =\n    phase21ClockSharkNow();\n\n  state.shifts[key] =\n    shift;\n\n  const employeeUpdates =\n    shift.status === \"open\"\n      ? {\n          clockSharkStatus:\n            \"clocked_in\",\n          clockSharkClockedIn:\n            true,\n          clockSharkCurrentJob:\n            shift.jobName ||\n            shift.trackingNumber ||\n            \"\",\n          clockSharkCurrentTrackingNumber:\n            shift.trackingNumber ||\n            \"\",\n          clockSharkClockInAt:\n            shift.clockInAt ||\n            phase21ClockSharkNow()\n        }\n      : {\n          clockSharkStatus:\n            \"clocked_out\",\n          clockSharkClockedIn:\n            false,\n          clockSharkCurrentJob:\n            \"\",\n          clockSharkCurrentTrackingNumber:\n            \"\",\n          clockSharkClockOutAt:\n            shift.clockOutAt ||\n            phase21ClockSharkNow(),\n          clockSharkLastHours:\n            shift.totalHours\n        };\n\n  phase21ClockSharkUpsertEmployee(\n    data,\n    state,\n    employee,\n    employeeUpdates\n  );\n\n  const matched =\n    phase21ClockSharkRecalculateWorkOrder(\n      data,\n      state,\n      shift.trackingNumber\n    );\n\n  if (\n    !matched &&\n    (\n      shift.trackingNumber ||\n      shift.jobName\n    )\n  ) {\n    phase21ClockSharkCreateTask(\n      data,\n      {\n        workflowType:\n          \"clockshark_unmatched_job\",\n        title:\n          \"Match ClockShark time to a Joshua work order\",\n        trackingNumber:\n          shift.trackingNumber ||\n          \"\",\n        assignedTo:\n          \"Ariana\",\n        priority:\n          \"urgent\",\n        notes:\n          [\n            shift.employeeName,\n            shift.jobName ||\n            shift.jobNumber,\n            shift.clockInAt\n              ? `Clock in: ${shift.clockInAt}`\n              : \"\"\n          ]\n            .filter(Boolean)\n            .join(\" · \"),\n        identity:\n          shift.id,\n        actionLabel:\n          \"Mark Job Matched\",\n        dueMinutes: 60\n      }\n    );\n  }\n\n  // JOSHUA_PHASE25_V7_TRUSTWORTHY_ALERTS\n  // Missing ClockShark notes are informational, not an operational exception.\n  // Notes are still imported and displayed when technicians enter them, but\n  // Joshua does not create a nuisance task merely because a shift has no note.\n\n\n  return {\n    shift,\n    employee,\n    job,\n    matched:\n      Boolean(matched)\n  };\n}\n\nfunction phase21ClockSharkApplyJob(\n  data,\n  state,\n  payload\n) {\n  const job =\n    phase21ClockSharkJob(\n      payload\n    );\n\n  const saved =\n    phase21ClockSharkUpsertJob(\n      state,\n      job\n    );\n\n  if (\n    job.trackingNumber &&\n    data.workOrders?.[\n      job.trackingNumber\n    ]\n  ) {\n    data.workOrders[\n      job.trackingNumber\n    ] = {\n      ...data.workOrders[\n        job.trackingNumber\n      ],\n      clockSharkJobId:\n        job.id,\n      clockSharkJobName:\n        job.name,\n      clockSharkJobStage:\n        job.stage,\n      clockSharkLastSyncAt:\n        phase21ClockSharkNow()\n    };\n  }\n\n  return {\n    job: saved\n  };\n}\n\nfunction phase21ClockSharkApplySchedule(\n  state,\n  payload\n) {\n  const schedule =\n    phase21ClockSharkSchedule(\n      payload\n    );\n\n  if (!schedule.id) {\n    return {\n      schedule: null\n    };\n  }\n\n  state.schedules[\n    schedule.id\n  ] = {\n    ...(state.schedules[\n      schedule.id\n    ] || {}),\n    ...schedule,\n    updatedAt:\n      phase21ClockSharkNow()\n  };\n\n  return {\n    schedule:\n      state.schedules[\n        schedule.id\n      ]\n  };\n}\n\nfunction phase21ClockSharkApplyNotification(\n  state,\n  payload\n) {\n  const item = {\n    id:\n      phase21ClockSharkText(\n        payload.id ||\n        payload.notificationId ||\n        payload.notification_id\n      ) ||\n      phase21ClockSharkId(\n        \"notification\"\n      ),\n    title:\n      phase21ClockSharkText(\n        payload.title ||\n        payload.subject ||\n        payload.type\n      ),\n    message:\n      phase21ClockSharkText(\n        payload.message ||\n        payload.body ||\n        payload.description ||\n        payload.text\n      ),\n    employeeName:\n      phase21ClockSharkEmployee(\n        payload\n      ).name,\n    jobName:\n      phase21ClockSharkJob(\n        payload\n      ).name,\n    trackingNumber:\n      phase21ClockSharkJob(\n        payload\n      ).trackingNumber,\n    createdAt:\n      phase21ClockSharkDate(\n        payload.createdAt ||\n        payload.created_at ||\n        payload.date\n      ) ||\n      phase21ClockSharkNow()\n  };\n\n  state.notifications.unshift(\n    item\n  );\n  state.notifications =\n    state.notifications.slice(\n      0,\n      500\n    );\n\n  return {\n    notification: item\n  };\n}\n\nfunction phase21ClockSharkRowsFromPayload(\n  payload\n) {\n  if (Array.isArray(payload)) {\n    return payload;\n  }\n\n  if (\n    payload &&\n    typeof payload === \"object\"\n  ) {\n    for (const key of [\n      \"events\",\n      \"items\",\n      \"rows\",\n      \"data\",\n      \"timeEntries\",\n      \"time_entries\",\n      \"shifts\"\n    ]) {\n      if (Array.isArray(payload[key])) {\n        return payload[key];\n      }\n    }\n  }\n\n  return [payload];\n}\n\nfunction phase21ClockSharkProcessOne(\n  data,\n  state,\n  payload,\n  forcedType = \"\"\n) {\n  const eventType =\n    phase21ClockSharkCanonicalType(\n      forcedType ||\n      payload?.eventType ||\n      payload?.event_type ||\n      payload?.event ||\n      payload?.type ||\n      payload?.trigger\n    );\n\n  const key =\n    phase21ClockSharkEventKey(\n      eventType,\n      payload || {}\n    );\n\n  if (\n    state.processedKeys.includes(\n      key\n    )\n  ) {\n    state.sync.duplicateCount =\n      Number(\n        state.sync.duplicateCount ||\n        0\n      ) + 1;\n\n    return {\n      duplicate: true,\n      eventType,\n      key\n    };\n  }\n\n  state.processedKeys.push(\n    key\n  );\n  state.processedKeys =\n    state.processedKeys.slice(\n      -5000\n    );\n\n  let result = {};\n\n  if (\n    [\"clock_in\", \"clock_out\"].includes(\n      eventType\n    )\n  ) {\n    result =\n      phase21ClockSharkApplyShift(\n        data,\n        state,\n        payload,\n        eventType\n      );\n  } else if (\n    eventType === \"job_added\"\n  ) {\n    result =\n      phase21ClockSharkApplyJob(\n        data,\n        state,\n        payload\n      );\n  } else if (\n    eventType ===\n    \"schedule_added\"\n  ) {\n    result =\n      phase21ClockSharkApplySchedule(\n        state,\n        payload\n      );\n  } else if (\n    eventType ===\n    \"notification\"\n  ) {\n    result =\n      phase21ClockSharkApplyNotification(\n        state,\n        payload\n      );\n  } else {\n    const shift =\n      phase21ClockSharkShift(\n        payload,\n        \"snapshot\"\n      );\n\n    if (\n      shift.employeeName ||\n      shift.clockInAt ||\n      shift.clockOutAt\n    ) {\n      result =\n        phase21ClockSharkApplyShift(\n          data,\n          state,\n          payload,\n          shift.clockOutAt\n            ? \"clock_out\"\n            : \"clock_in\"\n        );\n    } else {\n      result =\n        phase21ClockSharkApplyJob(\n          data,\n          state,\n          payload\n        );\n    }\n  }\n\n  const employeeName =\n    result.shift?.employeeName ||\n    result.job?.employeeName ||\n    result.schedule?.employeeName ||\n    result.notification?.employeeName ||\n    \"\";\n\n  const trackingNumber =\n    result.shift?.trackingNumber ||\n    result.job?.trackingNumber ||\n    result.schedule?.trackingNumber ||\n    result.notification?.trackingNumber ||\n    \"\";\n\n  const jobName =\n    result.shift?.jobName ||\n    result.job?.name ||\n    result.schedule?.jobName ||\n    result.notification?.jobName ||\n    \"\";\n\n  phase21ClockSharkAddEvent(\n    data,\n    state,\n    eventType,\n    payload,\n    {\n      employeeName,\n      trackingNumber,\n      jobName,\n      message:\n        [\n          \"ClockShark\",\n          eventType.replaceAll(\n            \"_\",\n            \" \"\n          ),\n          employeeName,\n          trackingNumber\n            ? `#${trackingNumber}`\n            : jobName\n        ]\n          .filter(Boolean)\n          .join(\" — \")\n    }\n  );\n\n  state.sync.processedCount =\n    Number(\n      state.sync.processedCount ||\n      0\n    ) + 1;\n  state.sync.lastEventAt =\n    phase21ClockSharkNow();\n  state.sync.lastSuccessAt =\n    phase21ClockSharkNow();\n  state.sync.lastError = \"\";\n\n  return {\n    duplicate: false,\n    eventType,\n    key,\n    ...result\n  };\n}\n\nfunction phase21ClockSharkProcessPayload(\n  data,\n  payload,\n  forcedType = \"\"\n) {\n  const state =\n    phase21ClockSharkEnsureData(\n      data\n    );\n\n  const results = [];\n\n  if (\n    payload &&\n    typeof payload === \"object\" &&\n    !Array.isArray(payload)\n  ) {\n    const groups = [\n      [\"employees\", \"employee\"],\n      [\"jobs\", \"job_added\"],\n      [\"schedules\", \"schedule_added\"],\n      [\"notifications\", \"notification\"],\n      [\"timeEntries\", \"snapshot\"],\n      [\"time_entries\", \"snapshot\"],\n      [\"shifts\", \"snapshot\"],\n      [\"events\", \"\"]\n    ];\n\n    let grouped = false;\n\n    for (const [key, type] of groups) {\n      if (\n        Array.isArray(payload[key])\n      ) {\n        grouped = true;\n\n        for (\n          const item of payload[key]\n        ) {\n          if (\n            key === \"employees\"\n          ) {\n            const employee =\n              phase21ClockSharkEmployee(\n                item\n              );\n\n            phase21ClockSharkUpsertEmployee(\n              data,\n              state,\n              employee\n            );\n\n            results.push({\n              duplicate: false,\n              eventType:\n                \"employee\",\n              employee\n            });\n          } else {\n            results.push(\n              phase21ClockSharkProcessOne(\n                data,\n                state,\n                item,\n                type ||\n                forcedType\n              )\n            );\n          }\n        }\n      }\n    }\n\n    if (grouped) {\n      return results;\n    }\n  }\n\n  for (\n    const item of\n    phase21ClockSharkRowsFromPayload(\n      payload\n    )\n  ) {\n    if (\n      !item ||\n      typeof item !== \"object\"\n    ) {\n      continue;\n    }\n\n    results.push(\n      phase21ClockSharkProcessOne(\n        data,\n        state,\n        item,\n        forcedType\n      )\n    );\n  }\n\n  return results;\n}\n\nfunction phase21ClockSharkInboundSecret() {\n  return phase21ClockSharkText(\n    process.env.CLOCKSHARK_INBOUND_SECRET\n  );\n}\n\nfunction phase21ClockSharkInboundAuthorized(\n  request\n) {\n  const expected =\n    phase21ClockSharkInboundSecret();\n\n  if (!expected) return false;\n\n  const bearer =\n    String(\n      request.headers.authorization ||\n      \"\"\n    ).startsWith(\"Bearer \")\n      ? String(\n          request.headers.authorization\n        ).slice(7)\n      : \"\";\n\n  const received =\n    phase21ClockSharkText(\n      request.headers[\n        \"x-clockshark-secret\"\n      ] ||\n      request.query?.secret ||\n      bearer\n    );\n\n  if (!received) return false;\n\n  const a =\n    Buffer.from(\n      expected,\n      \"utf8\"\n    );\n  const b =\n    Buffer.from(\n      received,\n      \"utf8\"\n    );\n\n  return (\n    a.length === b.length &&\n    crypto.timingSafeEqual(\n      a,\n      b\n    )\n  );\n}\n\nfunction phase21ClockSharkCsvRows(\n  csvText = \"\"\n) {\n  const text =\n    String(csvText || \"\")\n      .replace(/^\\uFEFF/, \"\");\n  const rows = [];\n  let row = [];\n  let field = \"\";\n  let quoted = false;\n\n  for (\n    let index = 0;\n    index < text.length;\n    index += 1\n  ) {\n    const character =\n      text[index];\n\n    if (quoted) {\n      if (\n        character === '\"' &&\n        text[index + 1] === '\"'\n      ) {\n        field += '\"';\n        index += 1;\n      } else if (\n        character === '\"'\n      ) {\n        quoted = false;\n      } else {\n        field += character;\n      }\n      continue;\n    }\n\n    if (character === '\"') {\n      quoted = true;\n    } else if (\n      character === \",\"\n    ) {\n      row.push(field);\n      field = \"\";\n    } else if (\n      character === \"\\n\"\n    ) {\n      row.push(field);\n      rows.push(row);\n      row = [];\n      field = \"\";\n    } else if (\n      character !== \"\\r\"\n    ) {\n      field += character;\n    }\n  }\n\n  if (\n    field ||\n    row.length\n  ) {\n    row.push(field);\n    rows.push(row);\n  }\n\n  return rows.filter(row =>\n    row.some(value =>\n      String(value).trim()\n    )\n  );\n}\n\nfunction phase21ClockSharkHeaderKey(\n  value = \"\"\n) {\n  return String(value || \"\")\n    .trim()\n    .toLowerCase()\n    .replace(/[^a-z0-9]+/g, \"\");\n}\n\nfunction phase21ClockSharkCsvObjects(\n  csvText = \"\"\n) {\n  const rows =\n    phase21ClockSharkCsvRows(\n      csvText\n    );\n\n  if (rows.length < 2) {\n    return [];\n  }\n\n  const headers =\n    rows[0].map(\n      phase21ClockSharkHeaderKey\n    );\n\n  return rows\n    .slice(1)\n    .map(row => {\n      const object = {};\n\n      headers.forEach(\n        (header, index) => {\n          if (!header) return;\n          object[header] =\n            row[index] ?? \"\";\n        }\n      );\n\n      return object;\n    });\n}\n\nfunction phase21ClockSharkCsvValue(\n  row,\n  keys = []\n) {\n  for (const key of keys) {\n    const normalized =\n      phase21ClockSharkHeaderKey(\n        key\n      );\n\n    if (\n      row[normalized] !==\n      undefined &&\n      String(\n        row[normalized]\n      ).trim() !== \"\"\n    ) {\n      return row[normalized];\n    }\n  }\n\n  return \"\";\n}\n\nfunction phase21ClockSharkCsvPayload(\n  row\n) {\n  const date =\n    phase21ClockSharkCsvValue(\n      row,\n      [\n        \"Date\",\n        \"Shift Date\",\n        \"Work Date\"\n      ]\n    );\n\n  const start =\n    phase21ClockSharkCsvValue(\n      row,\n      [\n        \"Clock In\",\n        \"Start Time\",\n        \"Time In\",\n        \"Shift Start\"\n      ]\n    );\n\n  const end =\n    phase21ClockSharkCsvValue(\n      row,\n      [\n        \"Clock Out\",\n        \"End Time\",\n        \"Time Out\",\n        \"Shift End\"\n      ]\n    );\n\n  const joinDateTime = value => {\n    const text =\n      phase21ClockSharkText(\n        value\n      );\n\n    if (!text) return \"\";\n\n    if (\n      /\\d{4}-\\d{2}-\\d{2}T/.test(\n        text\n      ) ||\n      /\\d{1,2}\\/\\d{1,2}\\/\\d{2,4}/.test(\n        text\n      )\n    ) {\n      return text;\n    }\n\n    return date\n      ? `${date} ${text}`\n      : text;\n  };\n\n  const firstName =\n    phase21ClockSharkCsvValue(\n      row,\n      [\n        \"Employee First Name\",\n        \"First Name\"\n      ]\n    );\n\n  const lastName =\n    phase21ClockSharkCsvValue(\n      row,\n      [\n        \"Employee Last Name\",\n        \"Last Name\"\n      ]\n    );\n\n  const employeeName =\n    phase21ClockSharkCsvValue(\n      row,\n      [\n        \"Employee\",\n        \"Employee Name\",\n        \"Full Name\",\n        \"Technician\"\n      ]\n    ) ||\n    [\n      firstName,\n      lastName\n    ].filter(Boolean).join(\" \");\n\n  return {\n    source:\n      \"ClockShark CSV Export\",\n    eventType: \"snapshot\",\n    id:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Shift ID\",\n          \"Time Entry ID\",\n          \"ID\"\n        ]\n      ),\n    employeeId:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Employee ID\",\n          \"User ID\"\n        ]\n      ),\n    employeeName,\n    employeeFirstName:\n      firstName,\n    employeeLastName:\n      lastName,\n    jobId:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Job ID\",\n          \"Project ID\"\n        ]\n      ),\n    jobNumber:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Job Number\",\n          \"Job No\",\n          \"Work Order\",\n          \"Tracking Number\"\n        ]\n      ),\n    jobName:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Job\",\n          \"Job Name\",\n          \"Project\"\n        ]\n      ),\n    taskName:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Task\",\n          \"Task Name\",\n          \"Task Code\"\n        ]\n      ),\n    clockInAt:\n      joinDateTime(start),\n    clockOutAt:\n      joinDateTime(end),\n    totalHours:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Total Hours\",\n          \"Total\",\n          \"Hours\",\n          \"Work Time\"\n        ]\n      ),\n    regularHours:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Regular Hours\",\n          \"Regular Time\"\n        ]\n      ),\n    overtimeHours:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Overtime Hours\",\n          \"Overtime\",\n          \"OT Hours\"\n        ]\n      ),\n    breakMinutes:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Break Minutes\",\n          \"Break Total\",\n          \"Break Duration\"\n        ]\n      ),\n    notes:\n      phase21ClockSharkCsvValue(\n        row,\n        [\n          \"Notes\",\n          \"Shift Notes\",\n          \"Clock Out Notes\"\n        ]\n      )\n  };\n}\n\nfunction phase21ClockSharkRunReconciliation(\n  data\n) {\n  const state =\n    phase21ClockSharkEnsureData(\n      data\n    );\n  const now = Date.now();\n  const staleMilliseconds =\n    PHASE21_CLOCKSHARK_STALE_HOURS *\n    60 *\n    60 *\n    1000;\n\n  let stale = 0;\n  let mismatches = 0;\n  let unmatched = 0;\n\n  for (\n    const shift of\n    Object.values(state.shifts)\n  ) {\n    if (\n      shift.status === \"open\" &&\n      shift.clockInAt &&\n      now -\n        new Date(\n          shift.clockInAt\n        ).getTime() >\n        staleMilliseconds\n    ) {\n      stale += 1;\n\n      phase21ClockSharkCreateTask(\n        data,\n        {\n          workflowType:\n            \"clockshark_stale_clock\",\n          title:\n            \"Verify stale ClockShark clock-in\",\n          trackingNumber:\n            shift.trackingNumber ||\n            \"\",\n          assignedTo:\n            \"Ariana\",\n          priority:\n            \"urgent\",\n          notes:\n            `${shift.employeeName || \"Employee\"} has remained clocked in since ${shift.clockInAt}.`,\n          identity:\n            shift.id,\n          actionLabel:\n            \"Resolve Clock Status\",\n          dueMinutes: 30\n        }\n      );\n    }\n\n    if (\n      !shift.trackingNumber ||\n      !Object.values(\n        data.workOrders || {}\n      ).some(workOrder =>\n        String(\n          workOrder.trackingNumber ||\n          workOrder.workOrderNumber ||\n          \"\"\n        ) ===\n        String(\n          shift.trackingNumber ||\n          \"\"\n        )\n      )\n    ) {\n      unmatched += 1;\n    }\n\n    if (\n      shift.trackingNumber\n    ) {\n      const workOrder =\n        Object.values(\n          data.workOrders || {}\n        ).find(item =>\n          String(\n            item.trackingNumber ||\n            item.workOrderNumber ||\n            \"\"\n          ) ===\n          String(\n            shift.trackingNumber\n          )\n        );\n\n      const workOrderSource = [\n        workOrder?.sourceSystem,\n        workOrder?.source,\n        workOrder?.integrationSource,\n        workOrder?.provider\n      ]\n        .filter(Boolean)\n        .join(\" \")\n        .toLowerCase();\n\n      const isServiceChannelWorkOrder = Boolean(\n        workOrder &&\n        (\n          workOrder.serviceChannelSourceOfTruth === true ||\n          workOrder.isServiceChannel === true ||\n          workOrder.serviceChannelPrimaryStatus ||\n          workOrder.serviceChannelExtendedStatus ||\n          workOrder.serviceChannelCheckInEventAt ||\n          workOrder.serviceChannelCheckOutEventAt ||\n          workOrderSource.includes(\"servicechannel\")\n        )\n      );\n\n      if (\n        workOrder &&\n        isServiceChannelWorkOrder &&\n        shift.status === \"open\" &&\n        ![\n          \"onsite\",\n          \"in_progress\"\n        ].includes(\n          String(\n            workOrder.joshuaStatus ||\n            workOrder.state ||\n            \"\"\n          ).toLowerCase()\n        ) &&\n        shift.clockInAt &&\n        now -\n          new Date(\n            shift.clockInAt\n          ).getTime() >\n          30 * 60000\n      ) {\n        mismatches += 1;\n\n        phase21ClockSharkCreateTask(\n          data,\n          {\n            workflowType:\n              \"clockshark_servicechannel_mismatch\",\n            title:\n              \"Resolve ClockShark and ServiceChannel status mismatch\",\n            trackingNumber:\n              shift.trackingNumber,\n            assignedTo:\n              \"Ariana\",\n            priority:\n              \"urgent\",\n            notes:\n              `${shift.employeeName || \"Employee\"} is clocked into ClockShark, but Joshua does not show the work order onsite.`,\n            identity:\n              shift.id,\n            actionLabel:\n              \"Resolve Status Mismatch\",\n            dueMinutes: 30\n          }\n        );\n      }\n    }\n  }\n\n  state.sync.lastReconciledAt =\n    phase21ClockSharkNow();\n  state.sync.lastReconciliation = {\n    stale,\n    mismatches,\n    unmatched\n  };\n\n  return {\n    stale,\n    mismatches,\n    unmatched\n  };\n}\n\nasync function phase21ClockSharkPullFeed() {\n  const feedUrl =\n    phase21ClockSharkText(\n      process.env.CLOCKSHARK_OPERATIONS_FEED_URL\n    );\n\n  if (!feedUrl) {\n    return {\n      ok: false,\n      skipped: true,\n      error:\n        \"CLOCKSHARK_OPERATIONS_FEED_URL is not configured.\"\n    };\n  }\n\n  const token =\n    phase21ClockSharkText(\n      process.env.CLOCKSHARK_OPERATIONS_FEED_TOKEN\n    );\n\n  const response =\n    await fetch(\n      feedUrl,\n      {\n        headers: {\n          accept:\n            \"application/json\",\n          ...(token\n            ? {\n                authorization:\n                  `Bearer ${token}`\n              }\n            : {})\n        }\n      }\n    );\n\n  const text =\n    await response.text();\n\n  if (!response.ok) {\n    throw new Error(\n      `ClockShark operations feed failed (${response.status}): ${text.slice(0, 500)}`\n    );\n  }\n\n  const payload =\n    text\n      ? JSON.parse(text)\n      : {};\n\n  const data =\n    readControlData();\n  const state =\n    phase21ClockSharkEnsureData(\n      data\n    );\n\n  const results =\n    phase21ClockSharkProcessPayload(\n      data,\n      payload,\n      \"snapshot\"\n    );\n\n  phase21ClockSharkRunReconciliation(\n    data\n  );\n\n  state.sync.lastPullAt =\n    phase21ClockSharkNow();\n  state.sync.lastSuccessAt =\n    phase21ClockSharkNow();\n  state.sync.lastError = \"\";\n  state.sync.lastPullResultCount =\n    results.filter(\n      result => !result.duplicate\n    ).length;\n\n  writeControlData(data);\n\n  return {\n    ok: true,\n    processed:\n      state.sync.lastPullResultCount,\n    duplicates:\n      results.filter(\n        result => result.duplicate\n      ).length\n  };\n}\n\nfunction phase21ClockSharkStatusPayload() {\n  const data =\n    readControlData();\n  const state =\n    phase21ClockSharkEnsureData(\n      data\n    );\n\n  const reconciliation =\n    phase21ClockSharkRunReconciliation(\n      data\n    );\n\n  writeControlData(data);\n\n  const shifts =\n    Object.values(state.shifts)\n      .sort(\n        (a, b) =>\n          new Date(\n            b.clockOutAt ||\n            b.clockInAt ||\n            b.updatedAt ||\n            0\n          ).getTime() -\n          new Date(\n            a.clockOutAt ||\n            a.clockInAt ||\n            a.updatedAt ||\n            0\n          ).getTime()\n      );\n\n  const today =\n    phase21ClockSharkLocalDate();\n  const weekStart =\n    phase21ClockSharkWeekStart();\n\n  const closedToday =\n    shifts.filter(shift =>\n      shift.status === \"closed\" &&\n      phase21ClockSharkLocalDate(\n        shift.clockOutAt ||\n        shift.clockInAt\n      ) === today\n    );\n\n  const closedThisWeek =\n    shifts.filter(shift => {\n      if (\n        shift.status !== \"closed\"\n      ) {\n        return false;\n      }\n\n      const date =\n        phase21ClockSharkLocalDate(\n          shift.clockOutAt ||\n          shift.clockInAt\n        );\n\n      return (\n        date &&\n        date >= weekStart\n      );\n    });\n\n  const currentlyClockedIn =\n    shifts.filter(shift =>\n      shift.status === \"open\"\n    );\n\n  const laborByJobMap =\n    new Map();\n\n  for (const shift of shifts) {\n    const key =\n      shift.trackingNumber ||\n      shift.jobNumber ||\n      shift.jobName ||\n      \"Unmatched ClockShark Job\";\n\n    const current =\n      laborByJobMap.get(key) || {\n        key,\n        trackingNumber:\n          shift.trackingNumber ||\n          \"\",\n        jobName:\n          shift.jobName ||\n          shift.jobNumber ||\n          \"Unmatched ClockShark Job\",\n        totalHours: 0,\n        regularHours: 0,\n        overtimeHours: 0,\n        employees: [],\n        notes: [],\n        shiftCount: 0,\n        openShiftCount: 0\n      };\n\n    current.totalHours +=\n      phase21ClockSharkNumber(\n        shift.totalHours,\n        0\n      );\n    current.regularHours +=\n      phase21ClockSharkNumber(\n        shift.regularHours,\n        0\n      );\n    current.overtimeHours +=\n      phase21ClockSharkNumber(\n        shift.overtimeHours,\n        0\n      );\n    current.employees.push(\n      shift.employeeName\n    );\n    current.notes.push(\n      shift.notes\n    );\n    current.shiftCount += 1;\n\n    if (\n      shift.status === \"open\"\n    ) {\n      current.openShiftCount += 1;\n    }\n\n    laborByJobMap.set(\n      key,\n      current\n    );\n  }\n\n  const laborByJob =\n    [...laborByJobMap.values()]\n      .map(item => ({\n        ...item,\n        totalHours:\n          Number(\n            item.totalHours\n              .toFixed(2)\n          ),\n        regularHours:\n          Number(\n            item.regularHours\n              .toFixed(2)\n          ),\n        overtimeHours:\n          Number(\n            item.overtimeHours\n              .toFixed(2)\n          ),\n        employees:\n          phase21ClockSharkUnique(\n            item.employees\n          ),\n        notes:\n          phase21ClockSharkUnique(\n            item.notes\n          ).slice(-10)\n      }))\n      .sort(\n        (a, b) =>\n          b.totalHours -\n          a.totalHours\n      );\n\n  const openClockTasks =\n    (data.tasks || []).filter(task =>\n      ![\"closed\", \"completed\"].includes(\n        String(task.status || \"\")\n          .toLowerCase()\n      ) &&\n      String(task.workflowType || \"\")\n        .startsWith(\n          \"clockshark_\"\n        )\n    );\n\n  return {\n    ok: true,\n    phase: 21,\n    name:\n      \"ClockShark Live Operations Sync\",\n    configured: {\n      inbound:\n        state.sync.inboundConfigured,\n      feed:\n        state.sync.feedConfigured,\n      roster:\n        state.sync.rosterConfigured\n    },\n    webhookUrl:\n      `${publicBaseUrl}/api/integrations/clockshark/events`,\n    sync: {\n      ...state.sync,\n      syncMinutes:\n        PHASE21_CLOCKSHARK_SYNC_MINUTES,\n      staleHours:\n        PHASE21_CLOCKSHARK_STALE_HOURS\n    },\n    metrics: {\n      currentlyClockedIn:\n        currentlyClockedIn.length,\n      laborHoursToday:\n        Number(\n          closedToday\n            .reduce(\n              (sum, shift) =>\n                sum +\n                phase21ClockSharkNumber(\n                  shift.totalHours,\n                  0\n                ),\n              0\n            )\n            .toFixed(2)\n        ),\n      overtimeHoursThisWeek:\n        Number(\n          closedThisWeek\n            .reduce(\n              (sum, shift) =>\n                sum +\n                phase21ClockSharkNumber(\n                  shift.overtimeHours,\n                  0\n                ),\n              0\n            )\n            .toFixed(2)\n        ),\n      totalTimeEntries:\n        shifts.length,\n      unmatchedEntries:\n        reconciliation.unmatched,\n      staleClockIns:\n        reconciliation.stale,\n      statusMismatches:\n        reconciliation.mismatches,\n      openClockSharkTasks:\n        openClockTasks.length\n    },\n    currentlyClockedIn:\n      currentlyClockedIn.slice(\n        0,\n        100\n      ),\n    recentShifts:\n      shifts.slice(0, 200),\n    laborByJob:\n      laborByJob.slice(0, 200),\n    upcomingSchedules:\n      Object.values(\n        state.schedules\n      )\n        .filter(schedule =>\n          !schedule.startAt ||\n          new Date(\n            schedule.startAt\n          ).getTime() >=\n          Date.now() -\n          24 * 60 * 60 * 1000\n        )\n        .sort(\n          (a, b) =>\n            new Date(\n              a.startAt || 0\n            ).getTime() -\n            new Date(\n              b.startAt || 0\n            ).getTime()\n        )\n        .slice(0, 100),\n    recentNotifications:\n      state.notifications.slice(\n        0,\n        100\n      ),\n    exceptions:\n      openClockTasks.slice(\n        0,\n        100\n      ),\n    employees:\n      Object.values(\n        state.employees\n      )\n        .sort((a, b) =>\n          String(a.name || \"\")\n            .localeCompare(\n              String(b.name || \"\")\n            )\n        ),\n    jobs:\n      Object.values(\n        state.jobs\n      ).slice(0, 500),\n    recentEvents:\n      state.events.slice(0, 100),\n    imports:\n      state.imports.slice(0, 20)\n  };\n}";

  server = server.replace(
    helperAnchor,
    helpers +
      "\n\n" +
      helperAnchor
  );

  const routeAnchor =
    'app.get("/", async () => ({';

  if (!server.includes(routeAnchor)) {
    throw new Error(
      "Could not locate Joshua's route registration anchor for Phase 21."
    );
  }

  const routes = "/* JOSHUA_PHASE21_CLOCKSHARK_ROUTES_V1 */\napp.get(\n  \"/api/integrations/clockshark/health\",\n  async (request, reply) => {\n    return reply.send({\n      ok: true,\n      integration:\n        \"ClockShark Live Operations\",\n      inboundConfigured:\n        Boolean(\n          phase21ClockSharkInboundSecret()\n        ),\n      feedConfigured:\n        Boolean(\n          phase21ClockSharkText(\n            process.env.CLOCKSHARK_OPERATIONS_FEED_URL\n          )\n        )\n    });\n  }\n);\n\napp.post(\n  \"/api/integrations/clockshark/events\",\n  async (request, reply) => {\n    if (\n      !phase21ClockSharkInboundSecret()\n    ) {\n      return reply.code(503).send({\n        ok: false,\n        error:\n          \"CLOCKSHARK_INBOUND_SECRET is not configured.\"\n      });\n    }\n\n    if (\n      !phase21ClockSharkInboundAuthorized(\n        request\n      )\n    ) {\n      return reply.code(401).send({\n        ok: false,\n        error:\n          \"Invalid ClockShark integration secret.\"\n      });\n    }\n\n    const data =\n      readControlData();\n    const state =\n      phase21ClockSharkEnsureData(\n        data\n      );\n\n    try {\n      const eventType =\n        phase21ClockSharkCanonicalType(\n          request.query?.event ||\n          request.body?.eventType ||\n          request.body?.event_type ||\n          \"\"\n        );\n\n      const results =\n        phase21ClockSharkProcessPayload(\n          data,\n          request.body || {},\n          eventType\n        );\n\n      const reconciliation =\n        phase21ClockSharkRunReconciliation(\n          data\n        );\n\n      state.sync.lastWebhookAt =\n        phase21ClockSharkNow();\n      state.sync.lastSuccessAt =\n        phase21ClockSharkNow();\n      state.sync.lastError = \"\";\n\n      writeControlData(data);\n\n      return reply.send({\n        ok: true,\n        processed:\n          results.filter(\n            result =>\n              !result.duplicate\n          ).length,\n        duplicates:\n          results.filter(\n            result =>\n              result.duplicate\n          ).length,\n        reconciliation\n      });\n    } catch (error) {\n      state.sync.lastError =\n        error.message;\n      state.sync.lastErrorAt =\n        phase21ClockSharkNow();\n      writeControlData(data);\n\n      app.log.error(\n        error,\n        \"ClockShark webhook processing failed\"\n      );\n\n      return reply.code(500).send({\n        ok: false,\n        error: error.message\n      });\n    }\n  }\n);\n\napp.get(\n  \"/api/control/clockshark/status\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply.code(401).send({\n        ok: false,\n        error: \"Unauthorized\"\n      });\n    }\n\n    return reply.send(\n      phase21ClockSharkStatusPayload()\n    );\n  }\n);\n\napp.post(\n  \"/api/control/clockshark/sync\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply.code(401).send({\n        ok: false,\n        error: \"Unauthorized\"\n      });\n    }\n\n    try {\n      const result =\n        await phase21ClockSharkPullFeed();\n\n      return reply.send({\n        ...result,\n        status:\n          phase21ClockSharkStatusPayload()\n      });\n    } catch (error) {\n      const data =\n        readControlData();\n      const state =\n        phase21ClockSharkEnsureData(\n          data\n        );\n\n      state.sync.lastError =\n        error.message;\n      state.sync.lastErrorAt =\n        phase21ClockSharkNow();\n      writeControlData(data);\n\n      return reply.code(500).send({\n        ok: false,\n        error: error.message\n      });\n    }\n  }\n);\n\napp.post(\n  \"/api/control/clockshark/reconcile\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply.code(401).send({\n        ok: false,\n        error: \"Unauthorized\"\n      });\n    }\n\n    const data =\n      readControlData();\n    const result =\n      phase21ClockSharkRunReconciliation(\n        data\n      );\n\n    writeControlData(data);\n\n    return reply.send({\n      ok: true,\n      reconciliation: result,\n      status:\n        phase21ClockSharkStatusPayload()\n    });\n  }\n);\n\napp.post(\n  \"/api/control/clockshark/import-csv\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply.code(401).send({\n        ok: false,\n        error: \"Unauthorized\"\n      });\n    }\n\n    const csvText =\n      String(\n        request.body?.csvText ||\n        \"\"\n      );\n\n    if (!csvText.trim()) {\n      return reply.code(400).send({\n        ok: false,\n        error:\n          \"ClockShark CSV text is required.\"\n      });\n    }\n\n    const rows =\n      phase21ClockSharkCsvObjects(\n        csvText\n      );\n\n    if (!rows.length) {\n      return reply.code(400).send({\n        ok: false,\n        error:\n          \"No ClockShark data rows were found in the CSV.\"\n      });\n    }\n\n    const data =\n      readControlData();\n    const state =\n      phase21ClockSharkEnsureData(\n        data\n      );\n    const results = [];\n\n    for (const row of rows) {\n      const payload =\n        phase21ClockSharkCsvPayload(\n          row\n        );\n\n      if (\n        !payload.employeeName &&\n        !payload.jobName &&\n        !payload.clockInAt &&\n        !payload.clockOutAt\n      ) {\n        continue;\n      }\n\n      results.push(\n        phase21ClockSharkProcessOne(\n          data,\n          state,\n          payload,\n          \"snapshot\"\n        )\n      );\n    }\n\n    const reconciliation =\n      phase21ClockSharkRunReconciliation(\n        data\n      );\n\n    const importRecord = {\n      id:\n        phase21ClockSharkId(\n          \"clockshark-import\"\n        ),\n      importedAt:\n        phase21ClockSharkNow(),\n      importedBy:\n        request.phase20User\n          ?.displayName ||\n        \"Office\",\n      rowCount:\n        rows.length,\n      processed:\n        results.filter(\n          result =>\n            !result.duplicate\n        ).length,\n      duplicates:\n        results.filter(\n          result =>\n            result.duplicate\n        ).length\n    };\n\n    state.imports.unshift(\n      importRecord\n    );\n    state.imports =\n      state.imports.slice(\n        0,\n        50\n      );\n    state.sync.lastCsvImportAt =\n      importRecord.importedAt;\n    state.sync.lastSuccessAt =\n      importRecord.importedAt;\n    state.sync.lastError = \"\";\n\n    writeControlData(data);\n\n    return reply.send({\n      ok: true,\n      import: importRecord,\n      reconciliation,\n      status:\n        phase21ClockSharkStatusPayload()\n    });\n  }\n);";

  server = server.replace(
    routeAnchor,
    routes +
      "\n\n" +
      routeAnchor
  );

  const scheduleAnchor =
    "const port = Number(process.env.PORT || 3000);";

  if (!server.includes(scheduleAnchor)) {
    throw new Error(
      "Could not locate Joshua's startup anchor for Phase 21."
    );
  }

  const schedule = "/* JOSHUA_PHASE21_CLOCKSHARK_SCHEDULE_V1 */\nsetTimeout(() => {\n  const data =\n    readControlData();\n\n  phase21ClockSharkEnsureData(\n    data\n  );\n  phase21ClockSharkRunReconciliation(\n    data\n  );\n  writeControlData(data);\n\n  if (\n    phase21ClockSharkText(\n      process.env.CLOCKSHARK_OPERATIONS_FEED_URL\n    )\n  ) {\n    phase21ClockSharkPullFeed()\n      .catch(error =>\n        app.log.error(\n          error,\n          \"Initial ClockShark operations sync failed\"\n        )\n      );\n  }\n}, 45 * 1000);\n\nsetInterval(() => {\n  const data =\n    readControlData();\n\n  phase21ClockSharkEnsureData(\n    data\n  );\n  phase21ClockSharkRunReconciliation(\n    data\n  );\n  writeControlData(data);\n\n  if (\n    phase21ClockSharkText(\n      process.env.CLOCKSHARK_OPERATIONS_FEED_URL\n    )\n  ) {\n    phase21ClockSharkPullFeed()\n      .catch(error =>\n        app.log.error(\n          error,\n          \"Scheduled ClockShark operations sync failed\"\n        )\n      );\n  }\n}, PHASE21_CLOCKSHARK_SYNC_MINUTES * 60 * 1000);";

  server = server.replace(
    scheduleAnchor,
    schedule +
      "\n\n" +
      scheduleAnchor
  );

  fs.writeFileSync(
    serverPath,
    server
  );

  console.log(
    "Joshua Phase 21 ClockShark live operations backend installed."
  );
}

await import("./servicechannel-webhook-bootstrap.mjs");
