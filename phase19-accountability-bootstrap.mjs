import fs from "node:fs";

const serverPath = new URL("./server.js", import.meta.url);
const panelPath = new URL("./public/control-panel.html", import.meta.url);

let server = fs.readFileSync(serverPath, "utf8");
let panel = fs.readFileSync(panelPath, "utf8");

const SERVER_MARKER =
  "JOSHUA_PHASE19_EXECUTIVE_ACCOUNTABILITY_V1";
const PANEL_MARKER =
  "JOSHUA_PHASE19_ACCOUNTABILITY_DASHBOARD_V1";

if (!server.includes(SERVER_MARKER)) {
  const helperAnchor =
    'function controlAuthorized(request) {';

  if (!server.includes(helperAnchor)) {
    throw new Error(
      "Could not locate Joshua's control authorization anchor for Phase 19."
    );
  }

  const helpers = "/* JOSHUA_PHASE19_EXECUTIVE_ACCOUNTABILITY_V1 */\nconst PHASE19_TIME_ZONE =\n  process.env.BUSINESS_TIME_ZONE ||\n  \"America/Chicago\";\n\nconst PHASE19_DEFAULT_SETTINGS = {\n  enabled: true,\n  morningBriefingEnabled: true,\n  endOfDayBriefingEnabled: true,\n  morningBriefingTime: \"07:30\",\n  endOfDayBriefingTime: \"17:30\",\n  urgentAcknowledgeMinutes: 15,\n  normalAcknowledgeMinutes: 30,\n  urgentEscalationMinutes: 30,\n  normalEscalationMinutes: 120,\n  sweepMinutes: 5\n};\n\nfunction phase19Id(prefix = \"phase19\") {\n  return [\n    prefix,\n    Date.now(),\n    Math.random().toString(36).slice(2, 9)\n  ].join(\"-\");\n}\n\nfunction phase19Iso(value = new Date()) {\n  const date = value instanceof Date ? value : new Date(value);\n  return Number.isFinite(date.getTime())\n    ? date.toISOString()\n    : new Date().toISOString();\n}\n\nfunction phase19Date(value = \"\") {\n  const date = new Date(value);\n  return Number.isFinite(date.getTime()) ? date : null;\n}\n\nfunction phase19MinutesFrom(value, minutes) {\n  const start = phase19Date(value) || new Date();\n  return new Date(start.getTime() + Number(minutes || 0) * 60000).toISOString();\n}\n\nfunction phase19LocalParts(date = new Date()) {\n  const formatter = new Intl.DateTimeFormat(\"en-CA\", {\n    timeZone: PHASE19_TIME_ZONE,\n    year: \"numeric\",\n    month: \"2-digit\",\n    day: \"2-digit\",\n    hour: \"2-digit\",\n    minute: \"2-digit\",\n    hour12: false\n  });\n\n  const parts = Object.fromEntries(\n    formatter\n      .formatToParts(date)\n      .filter(part => part.type !== \"literal\")\n      .map(part => [part.type, part.value])\n  );\n\n  return {\n    date: [parts.year, parts.month, parts.day].join(\"-\"),\n    time: [parts.hour, parts.minute].join(\":\"),\n    minutes:\n      Number(parts.hour || 0) * 60 +\n      Number(parts.minute || 0)\n  };\n}\n\nfunction phase19TomorrowLocalDate() {\n  const tomorrow = new Date(Date.now() + 24 * 60 * 60 * 1000);\n  return phase19LocalParts(tomorrow).date;\n}\n\nfunction phase19TimeToMinutes(value = \"\") {\n  const match = String(value || \"\").match(/^(\\d{1,2}):(\\d{2})$/);\n  if (!match) return null;\n  const hours = Number(match[1]);\n  const minutes = Number(match[2]);\n  if (\n    !Number.isInteger(hours) ||\n    !Number.isInteger(minutes) ||\n    hours < 0 ||\n    hours > 23 ||\n    minutes < 0 ||\n    minutes > 59\n  ) {\n    return null;\n  }\n  return hours * 60 + minutes;\n}\n\nfunction phase19EnsureData(data) {\n  data.tasks = Array.isArray(data.tasks) ? data.tasks : [];\n  data.events = Array.isArray(data.events) ? data.events : [];\n  data.callbacks = Array.isArray(data.callbacks) ? data.callbacks : [];\n  data.workOrders =\n    data.workOrders && typeof data.workOrders === \"object\"\n      ? data.workOrders\n      : {};\n  data.technicians =\n    data.technicians && typeof data.technicians === \"object\"\n      ? data.technicians\n      : {};\n\n  data.accountability =\n    data.accountability &&\n    typeof data.accountability === \"object\"\n      ? data.accountability\n      : {};\n\n  const accountability = data.accountability;\n\n  accountability.settings = {\n    ...PHASE19_DEFAULT_SETTINGS,\n    ...(accountability.settings || {})\n  };\n\n  accountability.history = Array.isArray(accountability.history)\n    ? accountability.history\n    : [];\n\n  accountability.briefings = Array.isArray(accountability.briefings)\n    ? accountability.briefings\n    : [];\n\n  accountability.sentBriefingKeys = Array.isArray(\n    accountability.sentBriefingKeys\n  )\n    ? accountability.sentBriefingKeys\n    : [];\n\n  accountability.activatedAt =\n    accountability.activatedAt ||\n    new Date().toISOString();\n\n  accountability.notificationGraceUntil =\n    accountability.notificationGraceUntil ||\n    phase19MinutesFrom(new Date(), 15);\n\n  accountability.autoBriefingsBeginDate =\n    accountability.autoBriefingsBeginDate ||\n    phase19TomorrowLocalDate();\n\n  return accountability;\n}\n\nfunction phase19RecordHistory(\n  data,\n  {\n    type = \"activity\",\n    taskId = \"\",\n    trackingNumber = \"\",\n    assignedTo = \"\",\n    message = \"\",\n    level = \"info\",\n    source = \"Joshua Accountability\"\n  } = {}\n) {\n  const accountability = phase19EnsureData(data);\n  const item = {\n    id: phase19Id(\"history\"),\n    createdAt: new Date().toISOString(),\n    type,\n    taskId,\n    trackingNumber,\n    assignedTo,\n    message,\n    level,\n    source\n  };\n\n  accountability.history.unshift(item);\n  accountability.history =\n    accountability.history.slice(0, 1000);\n\n  data.events.unshift({\n    id: phase19Id(\"event\"),\n    createdAt: item.createdAt,\n    type: `accountability_${type}`,\n    level,\n    trackingNumber,\n    requestedBy: source,\n    assignedTo,\n    taskId,\n    message\n  });\n  data.events = data.events.slice(0, 500);\n\n  return item;\n}\n\nfunction phase19TaskHistory(task) {\n  task.accountabilityHistory =\n    Array.isArray(task.accountabilityHistory)\n      ? task.accountabilityHistory\n      : [];\n  return task.accountabilityHistory;\n}\n\nfunction phase19AddTaskHistory(\n  task,\n  type,\n  message,\n  actor = \"Joshua\"\n) {\n  const item = {\n    id: phase19Id(\"task-history\"),\n    createdAt: new Date().toISOString(),\n    type,\n    message,\n    actor\n  };\n\n  phase19TaskHistory(task).unshift(item);\n  task.accountabilityHistory =\n    task.accountabilityHistory.slice(0, 100);\n\n  return item;\n}\n\nfunction phase19WorkflowType(task = {}) {\n  const explicit = String(task.workflowType || \"\")\n    .trim()\n    .toLowerCase();\n\n  if (explicit) return explicit;\n\n  const title = String(task.title || \"\").toLowerCase();\n\n  if (/callback|return.*call|call.*customer/.test(title)) {\n    return \"callback\";\n  }\n  if (/customer.*update|update.*customer/.test(title)) {\n    return \"customer_update\";\n  }\n  if (/proposal|quote/.test(title)) return \"proposal\";\n  if (/authorization/.test(title)) return \"authorization\";\n  if (/parts/.test(title)) return \"parts\";\n  if (/return.*trip|schedule.*return/.test(title)) {\n    return \"return_trip\";\n  }\n  if (/documentation|photos|completion notes/.test(title)) {\n    return \"documentation\";\n  }\n  if (/billing|invoice|ready to bill/.test(title)) {\n    return \"billing\";\n  }\n  if (/confirmation/.test(title)) {\n    return \"pending_confirmation\";\n  }\n  if (/checkout/.test(title)) return \"checkout_review\";\n\n  return \"general\";\n}\n\nfunction phase19DueMinutes(task = {}) {\n  const type = phase19WorkflowType(task);\n  const urgent =\n    String(task.priority || \"\").toLowerCase() === \"urgent\";\n\n  const byType = {\n    callback: 30,\n    customer_update: 60,\n    checkout_review: 60,\n    authorization: 120,\n    proposal: 240,\n    parts: 480,\n    return_trip: 480,\n    documentation: 720,\n    pending_confirmation: 1440,\n    billing: 1440,\n    general: urgent ? 120 : 1440\n  };\n\n  return byType[type] || (urgent ? 120 : 1440);\n}\n\nfunction phase19EnsureTask(task, settings) {\n  const now = new Date();\n  const createdAt =\n    phase19Date(task.createdAt) || now;\n  const priority =\n    String(task.priority || \"normal\").toLowerCase();\n  const urgent = priority === \"urgent\";\n\n  task.createdAt = createdAt.toISOString();\n  task.priority = priority;\n  task.assignedTo =\n    String(task.assignedTo || \"Unassigned\").trim() ||\n    \"Unassigned\";\n  task.workflowType = phase19WorkflowType(task);\n\n  if (!task.accountabilityInitializedAt) {\n    task.accountabilityInitializedAt =\n      now.toISOString();\n    phase19AddTaskHistory(\n      task,\n      \"accountability_started\",\n      \"Joshua began tracking ownership, acknowledgement, completion and escalation.\"\n    );\n  }\n\n  const acknowledgeMinutes = urgent\n    ? Number(settings.urgentAcknowledgeMinutes || 15)\n    : Number(settings.normalAcknowledgeMinutes || 30);\n\n  const escalationMinutes = urgent\n    ? Number(settings.urgentEscalationMinutes || 30)\n    : Number(settings.normalEscalationMinutes || 120);\n\n  task.acknowledgeBy =\n    task.acknowledgeBy ||\n    phase19MinutesFrom(\n      task.accountabilityInitializedAt,\n      acknowledgeMinutes\n    );\n\n  task.escalationDueAt =\n    task.escalationDueAt ||\n    phase19MinutesFrom(\n      task.accountabilityInitializedAt,\n      escalationMinutes\n    );\n\n  task.dueAt =\n    task.dueAt ||\n    phase19MinutesFrom(\n      task.accountabilityInitializedAt,\n      phase19DueMinutes(task)\n    );\n\n  const isClosed = [\"closed\", \"completed\"].includes(\n    String(task.status || \"\").toLowerCase()\n  );\n\n  if (isClosed) {\n    task.status = \"closed\";\n    task.completedAt =\n      task.completedAt ||\n      task.closedAt ||\n      task.updatedAt ||\n      now.toISOString();\n    task.accountabilityStatus = \"completed\";\n\n    if (!task.accountabilityCompletionRecordedAt) {\n      task.accountabilityCompletionRecordedAt =\n        now.toISOString();\n      phase19AddTaskHistory(\n        task,\n        \"completed\",\n        \"Task was marked completed.\"\n      );\n    }\n\n    return task;\n  }\n\n  task.status = \"open\";\n\n  if (task.escalatedAt || task.overdueEscalatedAt) {\n    task.accountabilityStatus = \"escalated\";\n  } else if (task.acknowledgedAt) {\n    task.accountabilityStatus = \"acknowledged\";\n  } else {\n    task.accountabilityStatus =\n      \"awaiting_acknowledgement\";\n  }\n\n  return task;\n}\n\nfunction phase19OpenTaskMatches(\n  data,\n  trackingNumber,\n  workflowType\n) {\n  return data.tasks.some(task =>\n    String(task.trackingNumber || \"\") ===\n      String(trackingNumber || \"\") &&\n    String(phase19WorkflowType(task)) ===\n      String(workflowType || \"\") &&\n    ![\"closed\", \"completed\"].includes(\n      String(task.status || \"\").toLowerCase()\n    )\n  );\n}\n\nfunction phase19CreateTaskInData(\n  data,\n  settings,\n  {\n    title,\n    trackingNumber = \"\",\n    assignedTo,\n    priority = \"normal\",\n    workflowType = \"general\",\n    notes = \"\",\n    actionLabel = \"Mark Complete\"\n  }\n) {\n  if (\n    trackingNumber &&\n    phase19OpenTaskMatches(\n      data,\n      trackingNumber,\n      workflowType\n    )\n  ) {\n    return null;\n  }\n\n  const task = {\n    id: phase19Id(\"task\"),\n    createdAt: new Date().toISOString(),\n    status: \"open\",\n    title,\n    trackingNumber: String(trackingNumber || \"\"),\n    assignedTo,\n    priority,\n    workflowType,\n    notes,\n    actionLabel,\n    source: \"Phase 19 Accountability\"\n  };\n\n  phase19EnsureTask(task, settings);\n  data.tasks.unshift(task);\n  data.tasks = data.tasks.slice(0, 500);\n\n  phase19RecordHistory(data, {\n    type: \"task_created\",\n    taskId: task.id,\n    trackingNumber: task.trackingNumber,\n    assignedTo: task.assignedTo,\n    message: `${task.title} was automatically assigned to ${task.assignedTo}.`,\n    level:\n      task.priority === \"urgent\"\n        ? \"warning\"\n        : \"info\"\n  });\n\n  return task;\n}\n\nfunction phase19DocumentationMissing(item = {}) {\n  const status = String(\n    item.joshuaDocumentation ||\n    item.documentationStatus ||\n    \"\"\n  ).toLowerCase();\n\n  return (\n    /missing/.test(status) ||\n    item.photosComplete === false ||\n    item.completionNotesComplete === false ||\n    String(item.joshuaStatus || item.state || \"\")\n      .toLowerCase() === \"documentation_missing\"\n  );\n}\n\nfunction phase19EnsureWorkflowTasks(data) {\n  const accountability = phase19EnsureData(data);\n  const settings = accountability.settings;\n  const now = Date.now();\n  const onsiteLimit =\n    Number(data.settings?.maxOnsiteMinutes || 240) *\n    60000;\n\n  for (const item of Object.values(data.workOrders)) {\n    const trackingNumber = String(\n      item.trackingNumber ||\n      item.workOrderNumber ||\n      \"\"\n    ).trim();\n\n    if (!trackingNumber) continue;\n\n    const state = String(\n      item.joshuaStatus ||\n      item.state ||\n      \"\"\n    ).toLowerCase();\n\n    if (state === \"pending_confirmation\") {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Confirm ServiceChannel completion\",\n        trackingNumber,\n        assignedTo: \"Ariana\",\n        priority: \"urgent\",\n        workflowType: \"pending_confirmation\",\n        actionLabel: \"Confirm Completion\",\n        notes:\n          \"Completed/Pending Confirmation cannot move to billing until completion is confirmed.\"\n      });\n    }\n\n    if (phase19DocumentationMissing(item)) {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Obtain missing completion documentation\",\n        trackingNumber,\n        assignedTo:\n          String(item.technician || \"\").trim() ||\n          \"Ariana\",\n        priority: \"urgent\",\n        workflowType: \"documentation\",\n        actionLabel: \"Mark Documentation Complete\",\n        notes:\n          \"Required photos or completion notes are missing. Billing remains blocked.\"\n      });\n    }\n\n    if (state === \"checkout_review\") {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Review unclear checkout outcome\",\n        trackingNumber,\n        assignedTo: \"Ariana\",\n        priority: \"urgent\",\n        workflowType: \"checkout_review\",\n        actionLabel: \"Mark Checkout Reviewed\",\n        notes:\n          \"Joshua could not safely determine the post-checkout workflow.\"\n      });\n    }\n\n    if (state === \"pending_proposal\") {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Prepare and submit proposal\",\n        trackingNumber,\n        assignedTo: \"Travis\",\n        priority: \"urgent\",\n        workflowType: \"proposal\",\n        actionLabel: \"Mark Proposal Submitted\",\n        notes:\n          item.nteExceeded\n            ? \"NTE is exceeded. Proposal is required.\"\n            : \"Proposal or quote workflow is pending.\"\n      });\n    }\n\n    if (state === \"awaiting_authorization\") {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Obtain customer or ServiceChannel authorization\",\n        trackingNumber,\n        assignedTo: \"Ariana\",\n        priority: \"urgent\",\n        workflowType: \"authorization\",\n        actionLabel: \"Mark Authorization Received\"\n      });\n    }\n\n    if (state === \"parts_needed\") {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Order parts and prepare return visit\",\n        trackingNumber,\n        assignedTo: \"Ariana\",\n        priority: \"urgent\",\n        workflowType: \"parts\",\n        actionLabel: \"Mark Parts Ordered\"\n      });\n    }\n\n    if (state === \"need_to_schedule\") {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Schedule return visit\",\n        trackingNumber,\n        assignedTo: \"Ariana\",\n        priority: \"urgent\",\n        workflowType: \"return_trip\",\n        actionLabel: \"Mark Return Scheduled\"\n      });\n    }\n\n    if (\n      state === \"ready_to_bill\" &&\n      ![\"submitted\", \"paid\"].includes(\n        String(item.invoiceStatus || \"\")\n          .toLowerCase()\n      )\n    ) {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Prepare and submit invoice\",\n        trackingNumber,\n        assignedTo: \"Shellie\",\n        priority: \"normal\",\n        workflowType: \"billing\",\n        actionLabel: \"Mark Invoice Submitted\",\n        notes:\n          \"ServiceChannel completion is confirmed and the job is eligible for billing.\"\n      });\n    }\n\n    if (\n      state === \"onsite\" &&\n      item.checkInAt &&\n      now -\n        new Date(item.checkInAt).getTime() >\n        onsiteLimit\n    ) {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Verify technician checkout or onsite status\",\n        trackingNumber,\n        assignedTo: \"Ariana\",\n        priority: \"urgent\",\n        workflowType: \"checkout_review\",\n        actionLabel: \"Resolve Onsite Status\",\n        notes:\n          \"The technician has remained marked onsite beyond the configured limit.\"\n      });\n    }\n\n    if (\n      [\n        \"pending_confirmation\",\n        \"awaiting_authorization\",\n        \"parts_needed\",\n        \"need_to_schedule\"\n      ].includes(state) &&\n      item.customerUpdated !== true\n    ) {\n      phase19CreateTaskInData(data, settings, {\n        title: \"Update customer on current job status\",\n        trackingNumber,\n        assignedTo: \"Ariana\",\n        priority: \"normal\",\n        workflowType: \"customer_update\",\n        actionLabel: \"Mark Customer Updated\",\n        notes:\n          \"Joshua has no confirmation that the customer received the latest status.\"\n      });\n    }\n  }\n\n  for (const callback of data.callbacks) {\n    const status = String(\n      callback.status || \"open\"\n    ).toLowerCase();\n\n    if ([\"closed\", \"completed\"].includes(status)) {\n      continue;\n    }\n\n    const callbackId = String(\n      callback.id ||\n      callback.callSid ||\n      callback.phone ||\n      callback.createdAt ||\n      \"\"\n    );\n\n    const trackingNumber = String(\n      callback.trackingNumber ||\n      callbackId\n    );\n\n    phase19CreateTaskInData(data, settings, {\n      title:\n        callback.title ||\n        `Return missed call${\n          callback.callerName\n            ? ` from ${callback.callerName}`\n            : \"\"\n        }`,\n      trackingNumber,\n      assignedTo:\n        callback.assignedTo ||\n        callback.owner ||\n        \"Ariana\",\n      priority:\n        callback.priority ||\n        \"urgent\",\n      workflowType: \"callback\",\n      actionLabel: \"Mark Callback Completed\",\n      notes:\n        callback.notes ||\n        callback.phone ||\n        \"Missed-call follow-up requires confirmation.\"\n    });\n  }\n\n  for (const task of data.tasks) {\n    phase19EnsureTask(task, settings);\n  }\n\n  return data;\n}\n\nfunction phase19TaskIsOverdue(task, now = Date.now()) {\n  const due = phase19Date(task.dueAt);\n  return Boolean(\n    due &&\n    due.getTime() < now &&\n    ![\"closed\", \"completed\"].includes(\n      String(task.status || \"\").toLowerCase()\n    )\n  );\n}\n\nfunction phase19PhoneForAssignee(\n  data,\n  assignedTo = \"\"\n) {\n  const name = String(assignedTo || \"\")\n    .trim()\n    .toLowerCase();\n\n  const member = teamSmsMembers().find(item =>\n    String(item.name || \"\")\n      .toLowerCase() === name\n  );\n\n  if (member?.number) return member.number;\n\n  const technician = Object.values(\n    data.technicians || {}\n  ).find(item =>\n    String(item.name || \"\")\n      .trim()\n      .toLowerCase() === name\n  );\n\n  return normalizePhone(\n    technician?.phone ||\n    technician?.mobile ||\n    \"\"\n  );\n}\n\nfunction phase19OwnerNumber() {\n  return (\n    teamSmsMembers().find(member =>\n      String(member.name || \"\")\n        .toLowerCase() === \"travis\"\n    )?.number ||\n    normalizePhone(\n      process.env.TRAVIS_SMS_NUMBER ||\n      process.env.OWNER_PHONE ||\n      \"+12142435649\"\n    )\n  );\n}\n\nasync function phase19SendSms(\n  to,\n  body\n) {\n  const number = normalizePhone(to);\n\n  if (\n    !twilioClient ||\n    !process.env.TWILIO_SMS_FROM ||\n    !number\n  ) {\n    return {\n      ok: false,\n      skipped: true,\n      reason: \"SMS is not configured.\"\n    };\n  }\n\n  try {\n    const message =\n      await twilioClient.messages.create({\n        from: process.env.TWILIO_SMS_FROM,\n        to: number,\n        body: String(body || \"\").slice(0, 1500)\n      });\n\n    return {\n      ok: true,\n      sid: message.sid,\n      to: number\n    };\n  } catch (error) {\n    app.log.error(\n      error,\n      \"Phase 19 SMS failed\"\n    );\n\n    return {\n      ok: false,\n      error: error.message,\n      to: number\n    };\n  }\n}\n\nasync function phase19SendOwnerEmail(\n  subject,\n  text\n) {\n  if (!mailTransport) {\n    return {\n      ok: false,\n      skipped: true,\n      reason: \"SMTP is not configured.\"\n    };\n  }\n\n  const to =\n    process.env.PHASE19_BRIEFING_EMAIL ||\n    process.env.NOTIFY_TO_EMAIL ||\n    process.env.OWNER_EMAIL ||\n    process.env.TRAVIS_EMAIL ||\n    \"Travis@ThePrecisionLighting.com\";\n\n  const from =\n    process.env.SMTP_FROM ||\n    process.env.SMTP_USER;\n\n  try {\n    const result = await mailTransport.sendMail({\n      from,\n      to,\n      subject,\n      text\n    });\n\n    return {\n      ok: true,\n      messageId: result.messageId,\n      to\n    };\n  } catch (error) {\n    app.log.error(\n      error,\n      \"Phase 19 owner email failed\"\n    );\n\n    return {\n      ok: false,\n      error: error.message,\n      to\n    };\n  }\n}\n\nfunction phase19OwnerLabel(task) {\n  return String(\n    task.assignedTo ||\n    \"Unassigned\"\n  ).trim();\n}\n\nfunction phase19BriefingData(\n  data,\n  type = \"morning\"\n) {\n  phase19EnsureWorkflowTasks(data);\n\n  const now = Date.now();\n  const local = phase19LocalParts();\n  const workOrders = Object.values(\n    data.workOrders || {}\n  );\n  const openTasks = data.tasks.filter(task =>\n    ![\"closed\", \"completed\"].includes(\n      String(task.status || \"\").toLowerCase()\n    )\n  );\n\n  const urgent = openTasks.filter(task =>\n    task.priority === \"urgent\" ||\n    task.accountabilityStatus === \"escalated\" ||\n    phase19TaskIsOverdue(task, now)\n  );\n\n  const onsite = workOrders.filter(item =>\n    String(\n      item.joshuaStatus ||\n      item.state ||\n      \"\"\n    ).toLowerCase() === \"onsite\"\n  );\n\n  const stalledStates = new Set([\n    \"pending_confirmation\",\n    \"pending_proposal\",\n    \"awaiting_authorization\",\n    \"parts_needed\",\n    \"need_to_schedule\",\n    \"documentation_missing\",\n    \"checkout_review\",\n    \"ready_to_bill\"\n  ]);\n\n  const stalled = workOrders.filter(item =>\n    stalledStates.has(\n      String(\n        item.joshuaStatus ||\n        item.state ||\n        \"\"\n      ).toLowerCase()\n    )\n  );\n\n  const callbacks = openTasks.filter(task =>\n    phase19WorkflowType(task) === \"callback\"\n  );\n\n  const proposals = openTasks.filter(task =>\n    phase19WorkflowType(task) === \"proposal\"\n  );\n\n  const billing = openTasks.filter(task =>\n    phase19WorkflowType(task) === \"billing\"\n  );\n\n  const customerUpdates = openTasks.filter(task =>\n    phase19WorkflowType(task) ===\n    \"customer_update\"\n  );\n\n  const documentation = openTasks.filter(task =>\n    phase19WorkflowType(task) ===\n    \"documentation\"\n  );\n\n  const overdue = openTasks.filter(task =>\n    phase19TaskIsOverdue(task, now)\n  );\n\n  const completedToday = data.tasks.filter(task =>\n    task.completedAt &&\n    phase19LocalParts(\n      new Date(task.completedAt)\n    ).date === local.date\n  );\n\n  const jobsCompletedToday = workOrders.filter(item =>\n    item.checkOutAt &&\n    phase19LocalParts(\n      new Date(item.checkOutAt)\n    ).date === local.date\n  );\n\n  const lines = [];\n  const heading =\n    type === \"eod\"\n      ? \"JOSHUA END-OF-DAY ACCOUNTABILITY REPORT\"\n      : \"JOSHUA MORNING OPERATIONS BRIEFING\";\n\n  lines.push(heading);\n  lines.push(local.date);\n  lines.push(\"\");\n\n  if (type === \"eod\") {\n    lines.push(\n      `Jobs checked out today: ${jobsCompletedToday.length}`\n    );\n    lines.push(\n      `Tasks completed today: ${completedToday.length}`\n    );\n    lines.push(\n      `Open items carrying forward: ${openTasks.length}`\n    );\n  } else {\n    lines.push(\n      `Urgent office actions: ${urgent.length}`\n    );\n    lines.push(\n      `Technicians currently onsite: ${onsite.length}`\n    );\n    lines.push(\n      `Stalled work orders: ${stalled.length}`\n    );\n  }\n\n  lines.push(\n    `Callbacks awaiting completion: ${callbacks.length}`\n  );\n  lines.push(\n    `Proposals awaiting action: ${proposals.length}`\n  );\n  lines.push(\n    `Ready-to-bill tasks: ${billing.length}`\n  );\n  lines.push(\n    `Customer updates pending: ${customerUpdates.length}`\n  );\n  lines.push(\n    `Documentation issues: ${documentation.length}`\n  );\n  lines.push(\n    `Overdue tasks: ${overdue.length}`\n  );\n\n  if (urgent.length) {\n    lines.push(\"\");\n    lines.push(\"URGENT OWNERSHIP\");\n    urgent.slice(0, 10).forEach(task => {\n      lines.push(\n        `• ${phase19OwnerLabel(task)} — ${task.title}` +\n        `${\n          task.trackingNumber\n            ? ` (#${task.trackingNumber})`\n            : \"\"\n        }`\n      );\n    });\n  }\n\n  if (openTasks.length) {\n    lines.push(\"\");\n    lines.push(\"OPEN ITEMS BY OWNER\");\n\n    const byOwner = new Map();\n\n    for (const task of openTasks) {\n      const owner = phase19OwnerLabel(task);\n      byOwner.set(\n        owner,\n        (byOwner.get(owner) || 0) + 1\n      );\n    }\n\n    for (const [owner, count] of byOwner) {\n      lines.push(`• ${owner}: ${count}`);\n    }\n  }\n\n  if (!openTasks.length && !onsite.length) {\n    lines.push(\"\");\n    lines.push(\n      \"No open accountability items are currently waiting.\"\n    );\n  }\n\n  return {\n    type,\n    date: local.date,\n    generatedAt: new Date().toISOString(),\n    heading,\n    counts: {\n      urgent: urgent.length,\n      onsite: onsite.length,\n      stalled: stalled.length,\n      callbacks: callbacks.length,\n      proposals: proposals.length,\n      billing: billing.length,\n      customerUpdates: customerUpdates.length,\n      documentation: documentation.length,\n      overdue: overdue.length,\n      openTasks: openTasks.length,\n      completedToday: completedToday.length,\n      jobsCompletedToday: jobsCompletedToday.length\n    },\n    text: lines.join(\"\\n\")\n  };\n}\n\nasync function phase19SendBriefing(\n  type = \"morning\",\n  {\n    manual = false\n  } = {}\n) {\n  const data = readControlData();\n  const accountability =\n    phase19EnsureData(data);\n  const briefing =\n    phase19BriefingData(data, type);\n\n  const key = [\n    briefing.date,\n    type\n  ].join(\"|\");\n\n  if (\n    !manual &&\n    accountability.sentBriefingKeys.includes(key)\n  ) {\n    return {\n      ok: true,\n      skipped: true,\n      duplicate: true,\n      briefing\n    };\n  }\n\n  const ownerNumber = phase19OwnerNumber();\n  const smsResult =\n    await phase19SendSms(\n      ownerNumber,\n      briefing.text\n    );\n\n  const emailResult =\n    await phase19SendOwnerEmail(\n      briefing.heading,\n      briefing.text\n    );\n\n  briefing.manual = manual;\n  briefing.sms = smsResult;\n  briefing.email = emailResult;\n\n  accountability.briefings.unshift(briefing);\n  accountability.briefings =\n    accountability.briefings.slice(0, 90);\n\n  if (\n    !accountability.sentBriefingKeys.includes(key)\n  ) {\n    accountability.sentBriefingKeys.push(key);\n    accountability.sentBriefingKeys =\n      accountability.sentBriefingKeys.slice(-180);\n  }\n\n  phase19RecordHistory(data, {\n    type: `${type}_briefing_sent`,\n    message:\n      `${briefing.heading} was delivered to Travis.` +\n      `${\n        manual\n          ? \" It was sent manually.\"\n          : \"\"\n      }`,\n    level: \"success\"\n  });\n\n  writeControlData(data);\n\n  return {\n    ok: true,\n    briefing,\n    sms: smsResult,\n    email: emailResult\n  };\n}\n\nasync function phase19RunSweep(\n  {\n    notify = true,\n    source = \"Scheduled Sweep\"\n  } = {}\n) {\n  const data = readControlData();\n  const accountability =\n    phase19EnsureData(data);\n  phase19EnsureWorkflowTasks(data);\n\n  const settings = accountability.settings;\n  const now = Date.now();\n  const graceUntil =\n    phase19Date(\n      accountability.notificationGraceUntil\n    )?.getTime() || 0;\n  const notifications = [];\n\n  for (const task of data.tasks) {\n    phase19EnsureTask(task, settings);\n\n    if (\n      [\"closed\", \"completed\"].includes(\n        String(task.status || \"\").toLowerCase()\n      )\n    ) {\n      continue;\n    }\n\n    const acknowledgeDue =\n      phase19Date(task.acknowledgeBy);\n    const escalationDue =\n      phase19Date(task.escalationDueAt);\n    const due =\n      phase19Date(task.dueAt);\n\n    if (\n      !task.acknowledgedAt &&\n      acknowledgeDue &&\n      now >= acknowledgeDue.getTime() &&\n      !task.acknowledgementReminderSentAt\n    ) {\n      task.acknowledgementReminderSentAt =\n        new Date().toISOString();\n\n      phase19AddTaskHistory(\n        task,\n        \"acknowledgement_reminder\",\n        `Joshua reminded ${task.assignedTo} to acknowledge the task.`\n      );\n\n      phase19RecordHistory(data, {\n        type: \"acknowledgement_reminder\",\n        taskId: task.id,\n        trackingNumber:\n          task.trackingNumber || \"\",\n        assignedTo: task.assignedTo,\n        message:\n          `${task.assignedTo} was reminded to acknowledge: ${task.title}`,\n        level: \"warning\"\n      });\n\n      notifications.push({\n        type: \"assignee\",\n        to: phase19PhoneForAssignee(\n          data,\n          task.assignedTo\n        ),\n        body:\n          `Joshua task acknowledgement required.\\n` +\n          `${task.title}` +\n          `${\n            task.trackingNumber\n              ? `\\nTracking #${task.trackingNumber}`\n              : \"\"\n          }\\nAssigned to: ${task.assignedTo}\\n` +\n          `Please acknowledge this task in the Joshua dashboard.`\n      });\n    }\n\n    if (\n      !task.acknowledgedAt &&\n      escalationDue &&\n      now >= escalationDue.getTime() &&\n      !task.escalatedAt\n    ) {\n      task.escalatedAt =\n        new Date().toISOString();\n      task.accountabilityStatus =\n        \"escalated\";\n\n      phase19AddTaskHistory(\n        task,\n        \"escalated\",\n        `The task was not acknowledged by ${task.assignedTo} and was escalated to Travis.`\n      );\n\n      phase19RecordHistory(data, {\n        type: \"task_escalated\",\n        taskId: task.id,\n        trackingNumber:\n          task.trackingNumber || \"\",\n        assignedTo: task.assignedTo,\n        message:\n          `${task.assignedTo} did not acknowledge: ${task.title}. Travis was notified.`,\n        level: \"error\"\n      });\n\n      notifications.push({\n        type: \"owner\",\n        to: phase19OwnerNumber(),\n        body:\n          `🚨 Joshua accountability escalation\\n` +\n          `${task.assignedTo} has not acknowledged:\\n` +\n          `${task.title}` +\n          `${\n            task.trackingNumber\n              ? `\\nTracking #${task.trackingNumber}`\n              : \"\"\n          }`\n      });\n    }\n\n    if (\n      task.acknowledgedAt &&\n      due &&\n      now >= due.getTime() &&\n      !task.overdueEscalatedAt\n    ) {\n      task.overdueEscalatedAt =\n        new Date().toISOString();\n      task.accountabilityStatus =\n        \"escalated\";\n\n      phase19AddTaskHistory(\n        task,\n        \"overdue\",\n        `The acknowledged task passed its completion deadline and was escalated to Travis.`\n      );\n\n      phase19RecordHistory(data, {\n        type: \"task_overdue\",\n        taskId: task.id,\n        trackingNumber:\n          task.trackingNumber || \"\",\n        assignedTo: task.assignedTo,\n        message:\n          `${task.assignedTo}'s task is overdue: ${task.title}`,\n        level: \"error\"\n      });\n\n      notifications.push({\n        type: \"owner\",\n        to: phase19OwnerNumber(),\n        body:\n          `🚨 Joshua overdue task\\n` +\n          `${task.assignedTo} acknowledged but has not completed:\\n` +\n          `${task.title}` +\n          `${\n            task.trackingNumber\n              ? `\\nTracking #${task.trackingNumber}`\n              : \"\"\n          }`\n      });\n\n      notifications.push({\n        type: \"assignee\",\n        to: phase19PhoneForAssignee(\n          data,\n          task.assignedTo\n        ),\n        body:\n          `Joshua reminder: this task is now overdue.\\n` +\n          `${task.title}` +\n          `${\n            task.trackingNumber\n              ? `\\nTracking #${task.trackingNumber}`\n              : \"\"\n          }\\nTravis has been notified.`\n      });\n    }\n  }\n\n  accountability.lastSweepAt =\n    new Date().toISOString();\n  accountability.lastSweepSource = source;\n  accountability.lastSweepNotificationCount =\n    notifications.length;\n\n  writeControlData(data);\n\n  const results = [];\n\n  if (\n    notify &&\n    now >= graceUntil\n  ) {\n    for (const notification of notifications) {\n      if (!notification.to) {\n        results.push({\n          ok: false,\n          skipped: true,\n          type: notification.type,\n          reason:\n            \"No phone number is available for the assigned person.\"\n        });\n        continue;\n      }\n\n      results.push(\n        await phase19SendSms(\n          notification.to,\n          notification.body\n        )\n      );\n    }\n  }\n\n  return {\n    ok: true,\n    notificationsPrepared:\n      notifications.length,\n    notificationsSent:\n      results.filter(result => result.ok).length,\n    graceActive:\n      now < graceUntil,\n    notificationResults: results\n  };\n}\n\nfunction phase19StatusPayload() {\n  const data = readControlData();\n  const accountability =\n    phase19EnsureData(data);\n  phase19EnsureWorkflowTasks(data);\n  writeControlData(data);\n\n  const now = Date.now();\n  const tasks = data.tasks\n    .filter(task =>\n      ![\"closed\", \"completed\"].includes(\n        String(task.status || \"\").toLowerCase()\n      )\n    )\n    .map(task => {\n      phase19EnsureTask(\n        task,\n        accountability.settings\n      );\n\n      return {\n        ...task,\n        overdue:\n          phase19TaskIsOverdue(task, now),\n        dueInMinutes: task.dueAt\n          ? Math.round(\n              (\n                new Date(task.dueAt).getTime() -\n                now\n              ) / 60000\n            )\n          : null\n      };\n    })\n    .sort((a, b) => {\n      const rank = task => {\n        if (task.accountabilityStatus === \"escalated\") {\n          return 0;\n        }\n        if (task.overdue) return 1;\n        if (\n          task.accountabilityStatus ===\n          \"awaiting_acknowledgement\"\n        ) {\n          return 2;\n        }\n        return 3;\n      };\n\n      return (\n        rank(a) - rank(b) ||\n        new Date(a.dueAt).getTime() -\n          new Date(b.dueAt).getTime()\n      );\n    });\n\n  const local = phase19LocalParts();\n\n  return {\n    ok: true,\n    phase: 19,\n    name:\n      \"Executive Accountability & Daily Operations Briefing\",\n    persistentDataFile: controlDataFile,\n    persistentStorageActive:\n      !String(controlDataFile).startsWith(\"/tmp/\"),\n    settings: accountability.settings,\n    activatedAt: accountability.activatedAt,\n    notificationGraceUntil:\n      accountability.notificationGraceUntil,\n    autoBriefingsBeginDate:\n      accountability.autoBriefingsBeginDate,\n    lastSweepAt: accountability.lastSweepAt || \"\",\n    metrics: {\n      open: tasks.length,\n      awaitingAcknowledgement:\n        tasks.filter(task =>\n          task.accountabilityStatus ===\n          \"awaiting_acknowledgement\"\n        ).length,\n      acknowledged:\n        tasks.filter(task =>\n          task.accountabilityStatus ===\n          \"acknowledged\"\n        ).length,\n      escalated:\n        tasks.filter(task =>\n          task.accountabilityStatus ===\n          \"escalated\"\n        ).length,\n      overdue:\n        tasks.filter(task => task.overdue).length,\n      completedToday:\n        data.tasks.filter(task =>\n          task.completedAt &&\n          phase19LocalParts(\n            new Date(task.completedAt)\n          ).date === local.date\n        ).length\n    },\n    tasks,\n    recentHistory:\n      accountability.history.slice(0, 100),\n    recentBriefings:\n      accountability.briefings.slice(0, 14)\n  };\n}\n\nasync function phase19MaybeSendScheduledBriefings() {\n  const data = readControlData();\n  const accountability =\n    phase19EnsureData(data);\n  const settings = accountability.settings;\n\n  if (!settings.enabled) return;\n\n  const local = phase19LocalParts();\n\n  if (\n    local.date <\n    accountability.autoBriefingsBeginDate\n  ) {\n    return;\n  }\n\n  const morning =\n    phase19TimeToMinutes(\n      settings.morningBriefingTime\n    );\n  const endOfDay =\n    phase19TimeToMinutes(\n      settings.endOfDayBriefingTime\n    );\n\n  if (\n    settings.morningBriefingEnabled &&\n    morning !== null &&\n    local.minutes >= morning &&\n    local.minutes <= 11 * 60\n  ) {\n    await phase19SendBriefing(\"morning\");\n  }\n\n  if (\n    settings.endOfDayBriefingEnabled &&\n    endOfDay !== null &&\n    local.minutes >= endOfDay &&\n    local.minutes <= 22 * 60\n  ) {\n    await phase19SendBriefing(\"eod\");\n  }\n}";

  server = server.replace(
    helperAnchor,
    helpers + "\n\n" + helperAnchor
  );

  const taskRouteAnchor =
    'app.post("/api/control/tasks", async (request, reply) => {';

  if (!server.includes(taskRouteAnchor)) {
    throw new Error(
      "Could not locate Joshua's task route for Phase 19."
    );
  }

  const routes = "/* JOSHUA_PHASE19_EXECUTIVE_ACCOUNTABILITY_ROUTES_V1 */\napp.get(\n  \"/api/control/accountability/status\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply\n        .code(401)\n        .send({\n          ok: false,\n          error: \"Unauthorized\"\n        });\n    }\n\n    return reply.send(\n      phase19StatusPayload()\n    );\n  }\n);\n\napp.post(\n  \"/api/control/accountability/run\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply\n        .code(401)\n        .send({\n          ok: false,\n          error: \"Unauthorized\"\n        });\n    }\n\n    const result = await phase19RunSweep({\n      notify: true,\n      source: \"Manual Dashboard Run\"\n    });\n\n    return reply.send({\n      ...result,\n      status: phase19StatusPayload()\n    });\n  }\n);\n\napp.post(\n  \"/api/control/accountability/briefing\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply\n        .code(401)\n        .send({\n          ok: false,\n          error: \"Unauthorized\"\n        });\n    }\n\n    const type =\n      String(\n        request.body?.type ||\n        \"morning\"\n      ).toLowerCase() === \"eod\"\n        ? \"eod\"\n        : \"morning\";\n\n    const result =\n      await phase19SendBriefing(type, {\n        manual: true\n      });\n\n    return reply.send(result);\n  }\n);\n\napp.post(\n  \"/api/control/accountability/settings\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply\n        .code(401)\n        .send({\n          ok: false,\n          error: \"Unauthorized\"\n        });\n    }\n\n    const data = readControlData();\n    const accountability =\n      phase19EnsureData(data);\n    const body = request.body || {};\n    const settings =\n      accountability.settings;\n\n    for (const key of [\n      \"enabled\",\n      \"morningBriefingEnabled\",\n      \"endOfDayBriefingEnabled\"\n    ]) {\n      if (body[key] !== undefined) {\n        settings[key] =\n          body[key] === true ||\n          body[key] === \"true\";\n      }\n    }\n\n    for (const key of [\n      \"morningBriefingTime\",\n      \"endOfDayBriefingTime\"\n    ]) {\n      if (body[key] !== undefined) {\n        const value =\n          String(body[key] || \"\").trim();\n\n        if (\n          phase19TimeToMinutes(value) === null\n        ) {\n          return reply\n            .code(400)\n            .send({\n              ok: false,\n              error:\n                `${key} must use HH:MM format.`\n            });\n        }\n\n        settings[key] = value;\n      }\n    }\n\n    for (const key of [\n      \"urgentAcknowledgeMinutes\",\n      \"normalAcknowledgeMinutes\",\n      \"urgentEscalationMinutes\",\n      \"normalEscalationMinutes\"\n    ]) {\n      if (body[key] !== undefined) {\n        const value = Number(body[key]);\n\n        if (\n          !Number.isFinite(value) ||\n          value < 5 ||\n          value > 1440\n        ) {\n          return reply\n            .code(400)\n            .send({\n              ok: false,\n              error:\n                `${key} must be between 5 and 1440 minutes.`\n            });\n        }\n\n        settings[key] = Math.round(value);\n      }\n    }\n\n    accountability.settings = settings;\n    writeControlData(data);\n\n    phase19RecordHistory(data, {\n      type: \"settings_updated\",\n      message:\n        \"Phase 19 accountability settings were updated from the dashboard.\",\n      level: \"success\",\n      source: \"Control Panel\"\n    });\n    writeControlData(data);\n\n    return reply.send({\n      ok: true,\n      settings\n    });\n  }\n);\n\napp.post(\n  \"/api/control/tasks/:id/acknowledge\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply\n        .code(401)\n        .send({\n          ok: false,\n          error: \"Unauthorized\"\n        });\n    }\n\n    const data = readControlData();\n    const accountability =\n      phase19EnsureData(data);\n    const task = data.tasks.find(item =>\n      String(item.id) ===\n      String(request.params.id)\n    );\n\n    if (!task) {\n      return reply\n        .code(404)\n        .send({\n          ok: false,\n          error: \"Task not found.\"\n        });\n    }\n\n    phase19EnsureTask(\n      task,\n      accountability.settings\n    );\n\n    const actor =\n      String(\n        request.body?.actor ||\n        task.assignedTo ||\n        \"Office\"\n      ).trim();\n\n    task.acknowledgedAt =\n      new Date().toISOString();\n    task.acknowledgedBy = actor;\n    task.accountabilityStatus =\n      \"acknowledged\";\n    task.updatedAt =\n      new Date().toISOString();\n\n    phase19AddTaskHistory(\n      task,\n      \"acknowledged\",\n      `${actor} acknowledged ownership of the task.`,\n      actor\n    );\n\n    phase19RecordHistory(data, {\n      type: \"task_acknowledged\",\n      taskId: task.id,\n      trackingNumber:\n        task.trackingNumber || \"\",\n      assignedTo: task.assignedTo,\n      message:\n        `${actor} acknowledged: ${task.title}`,\n      level: \"success\",\n      source: actor\n    });\n\n    writeControlData(data);\n\n    return reply.send({\n      ok: true,\n      task\n    });\n  }\n);\n\napp.post(\n  \"/api/control/tasks/:id/complete\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply\n        .code(401)\n        .send({\n          ok: false,\n          error: \"Unauthorized\"\n        });\n    }\n\n    const data = readControlData();\n    const accountability =\n      phase19EnsureData(data);\n    const task = data.tasks.find(item =>\n      String(item.id) ===\n      String(request.params.id)\n    );\n\n    if (!task) {\n      return reply\n        .code(404)\n        .send({\n          ok: false,\n          error: \"Task not found.\"\n        });\n    }\n\n    phase19EnsureTask(\n      task,\n      accountability.settings\n    );\n\n    const actor =\n      String(\n        request.body?.actor ||\n        task.assignedTo ||\n        \"Office\"\n      ).trim();\n\n    const completedAt =\n      new Date().toISOString();\n\n    task.status = \"closed\";\n    task.completedAt = completedAt;\n    task.closedAt = completedAt;\n    task.completedBy = actor;\n    task.accountabilityStatus =\n      \"completed\";\n    task.updatedAt = completedAt;\n    task.accountabilityCompletionRecordedAt =\n      completedAt;\n\n    phase19AddTaskHistory(\n      task,\n      \"completed\",\n      `${actor} marked the task completed.`,\n      actor\n    );\n\n    phase19RecordHistory(data, {\n      type: \"task_completed\",\n      taskId: task.id,\n      trackingNumber:\n        task.trackingNumber || \"\",\n      assignedTo: task.assignedTo,\n      message:\n        `${actor} completed: ${task.title}`,\n      level: \"success\",\n      source: actor\n    });\n\n    writeControlData(data);\n\n    return reply.send({\n      ok: true,\n      task\n    });\n  }\n);\n\napp.post(\n  \"/api/control/tasks/:id/reassign\",\n  async (request, reply) => {\n    if (!controlAuthorized(request)) {\n      return reply\n        .code(401)\n        .send({\n          ok: false,\n          error: \"Unauthorized\"\n        });\n    }\n\n    const assignedTo =\n      String(\n        request.body?.assignedTo ||\n        \"\"\n      ).trim();\n\n    if (!assignedTo) {\n      return reply\n        .code(400)\n        .send({\n          ok: false,\n          error:\n            \"A new assigned person is required.\"\n        });\n    }\n\n    const data = readControlData();\n    const accountability =\n      phase19EnsureData(data);\n    const task = data.tasks.find(item =>\n      String(item.id) ===\n      String(request.params.id)\n    );\n\n    if (!task) {\n      return reply\n        .code(404)\n        .send({\n          ok: false,\n          error: \"Task not found.\"\n        });\n    }\n\n    const previous =\n      task.assignedTo || \"Unassigned\";\n\n    task.assignedTo = assignedTo;\n    task.acknowledgedAt = \"\";\n    task.acknowledgedBy = \"\";\n    task.escalatedAt = \"\";\n    task.overdueEscalatedAt = \"\";\n    task.acknowledgementReminderSentAt = \"\";\n    task.accountabilityInitializedAt =\n      new Date().toISOString();\n    task.acknowledgeBy = \"\";\n    task.escalationDueAt = \"\";\n    task.accountabilityStatus =\n      \"awaiting_acknowledgement\";\n    task.updatedAt =\n      new Date().toISOString();\n\n    phase19EnsureTask(\n      task,\n      accountability.settings\n    );\n\n    phase19AddTaskHistory(\n      task,\n      \"reassigned\",\n      `Task ownership changed from ${previous} to ${assignedTo}.`,\n      \"Office\"\n    );\n\n    phase19RecordHistory(data, {\n      type: \"task_reassigned\",\n      taskId: task.id,\n      trackingNumber:\n        task.trackingNumber || \"\",\n      assignedTo,\n      message:\n        `${task.title} was reassigned from ${previous} to ${assignedTo}.`,\n      level: \"warning\",\n      source: \"Control Panel\"\n    });\n\n    writeControlData(data);\n\n    return reply.send({\n      ok: true,\n      task\n    });\n  }\n);";

  server = server.replace(
    taskRouteAnchor,
    routes + "\n\n" + taskRouteAnchor
  );

  const portAnchor =
    'const port = Number(process.env.PORT || 3000);';

  if (!server.includes(portAnchor)) {
    throw new Error(
      "Could not locate Joshua's startup anchor for Phase 19."
    );
  }

  const schedule = "/* JOSHUA_PHASE19_EXECUTIVE_ACCOUNTABILITY_SCHEDULE_V1 */\nsetTimeout(() => {\n  phase19RunSweep({\n    notify: false,\n    source: \"Initial Startup Preparation\"\n  }).catch(error =>\n    app.log.error(\n      error,\n      \"Initial Phase 19 preparation failed\"\n    )\n  );\n}, 20 * 1000);\n\nsetInterval(() => {\n  phase19RunSweep({\n    notify: true,\n    source: \"Scheduled Accountability Sweep\"\n  }).catch(error =>\n    app.log.error(\n      error,\n      \"Phase 19 accountability sweep failed\"\n    )\n  );\n\n  phase19MaybeSendScheduledBriefings()\n    .catch(error =>\n      app.log.error(\n        error,\n        \"Phase 19 scheduled briefing failed\"\n      )\n    );\n}, 5 * 60 * 1000);\n";

  server = server.replace(
    portAnchor,
    schedule + "\n\n" + portAnchor
  );

  fs.writeFileSync(serverPath, server);
  console.log(
    "Joshua Phase 19 executive accountability backend installed."
  );
}

if (!panel.includes(PANEL_MARKER)) {
  const css = "/* JOSHUA_PHASE19_ACCOUNTABILITY_DASHBOARD_V1 */\n.phase19-stat-grid{\n  display:grid;\n  grid-template-columns:repeat(5,minmax(130px,1fr));\n  gap:12px;\n}\n.phase19-stat{\n  padding:14px;\n  border:1px solid #30445d;\n  border-radius:12px;\n  background:#0f1925;\n}\n.phase19-stat strong{\n  display:block;\n  font-size:25px;\n  margin-top:4px;\n}\n.phase19-toolbar{\n  display:flex;\n  gap:8px;\n  flex-wrap:wrap;\n}\n.phase19-toolbar button{\n  width:auto;\n}\n.phase19-task-list{\n  display:grid;\n  gap:10px;\n  max-height:680px;\n  overflow:auto;\n}\n.phase19-task{\n  border:1px solid #2d4158;\n  border-radius:12px;\n  padding:13px;\n  background:#0f1925;\n}\n.phase19-task.escalated{\n  border-color:#a64b3d;\n}\n.phase19-task.overdue{\n  box-shadow:inset 4px 0 #d76c57;\n}\n.phase19-task-head{\n  display:flex;\n  justify-content:space-between;\n  gap:12px;\n  align-items:flex-start;\n}\n.phase19-task-actions{\n  display:flex;\n  gap:8px;\n  flex-wrap:wrap;\n  margin-top:10px;\n}\n.phase19-task-actions button{\n  width:auto;\n}\n.phase19-history{\n  max-height:520px;\n  overflow:auto;\n}\n.phase19-history-row{\n  padding:10px 0;\n  border-bottom:1px solid #28384c;\n}\n.phase19-status{\n  display:inline-block;\n  padding:5px 9px;\n  border-radius:999px;\n  background:#34475e;\n  text-transform:capitalize;\n  font-size:11px;\n}\n.phase19-status.awaiting_acknowledgement{\n  background:#6a521d;\n}\n.phase19-status.acknowledged{\n  background:#185a41;\n}\n.phase19-status.escalated{\n  background:#753d32;\n}\n.phase19-storage-ok{\n  color:#62e3a6;\n  font-weight:800;\n}\n.phase19-storage-warning{\n  color:#ff9f8f;\n  font-weight:800;\n}\n@media(max-width:900px){\n  .phase19-stat-grid{\n    grid-template-columns:repeat(2,minmax(0,1fr));\n  }\n  .phase19-task-head{\n    flex-direction:column;\n  }\n}\n@media(max-width:520px){\n  .phase19-stat-grid{\n    grid-template-columns:1fr;\n  }\n  .phase19-toolbar button,\n  .phase19-task-actions button{\n    width:100%;\n  }\n}";

  if (!panel.includes("</style>")) {
    throw new Error(
      "Could not locate the dashboard style section for Phase 19."
    );
  }

  panel = panel.replace(
    "</style>",
    "\n" + css + "\n</style>"
  );

  const taskTab =
    ' <button class="tab" data-tab="tasks">Tasks</button>';

  if (!panel.includes(taskTab)) {
    throw new Error(
      "Could not locate the dashboard Tasks tab for Phase 19."
    );
  }

  panel = panel.replace(
    taskTab,
    taskTab +
      '\n <button class="tab" data-tab="accountability">Accountability</button>'
  );

  const activityAnchor =
    '<section id="activity" class="panel">';

  if (!panel.includes(activityAnchor)) {
    throw new Error(
      "Could not locate the dashboard Activity panel for Phase 19."
    );
  }

  const section = "<section id=\"accountability\" class=\"panel\">\n  <div class=\"card\">\n    <div class=\"phase19-task-head\">\n      <div>\n        <h2>Executive Accountability</h2>\n        <div class=\"small muted\">\n          Every callback, proposal, customer update, work order and invoice has an owner.\n        </div>\n        <div id=\"phase19StorageStatus\" class=\"small\" style=\"margin-top:6px\"></div>\n      </div>\n      <div class=\"phase19-toolbar\">\n        <button type=\"button\" id=\"phase19RunNow\">Run Accountability Now</button>\n        <button type=\"button\" class=\"secondary\" id=\"phase19MorningBriefing\">Send Morning Briefing</button>\n        <button type=\"button\" class=\"secondary\" id=\"phase19EodBriefing\">Send End-of-Day Briefing</button>\n      </div>\n    </div>\n    <div id=\"phase19Message\" class=\"small muted\" style=\"margin-top:10px\"></div>\n  </div>\n\n  <div class=\"phase19-stat-grid\" style=\"margin-top:14px\">\n    <div class=\"phase19-stat\">\n      <span class=\"muted\">Awaiting acknowledgement</span>\n      <strong id=\"phase19Awaiting\">0</strong>\n    </div>\n    <div class=\"phase19-stat\">\n      <span class=\"muted\">Acknowledged</span>\n      <strong id=\"phase19Acknowledged\">0</strong>\n    </div>\n    <div class=\"phase19-stat\">\n      <span class=\"muted\">Escalated</span>\n      <strong id=\"phase19Escalated\">0</strong>\n    </div>\n    <div class=\"phase19-stat\">\n      <span class=\"muted\">Overdue</span>\n      <strong id=\"phase19Overdue\">0</strong>\n    </div>\n    <div class=\"phase19-stat\">\n      <span class=\"muted\">Completed today</span>\n      <strong id=\"phase19CompletedToday\">0</strong>\n    </div>\n  </div>\n\n  <div class=\"grid two\" style=\"margin-top:14px\">\n    <div class=\"card\">\n      <h2>Owned Open Tasks</h2>\n      <div id=\"phase19TaskList\" class=\"phase19-task-list\">\n        <div class=\"muted\">Loading accountability tasks…</div>\n      </div>\n    </div>\n\n    <div class=\"grid\">\n      <div class=\"card\">\n        <h2>Daily Briefing Schedule</h2>\n        <form id=\"phase19SettingsForm\">\n          <div class=\"formgrid\">\n            <div>\n              <label>Morning briefing</label>\n              <input id=\"phase19MorningTime\" type=\"time\" value=\"07:30\">\n            </div>\n            <div>\n              <label>End-of-day briefing</label>\n              <input id=\"phase19EodTime\" type=\"time\" value=\"17:30\">\n            </div>\n            <div>\n              <label>Urgent acknowledgement (minutes)</label>\n              <input id=\"phase19UrgentAck\" type=\"number\" min=\"5\" max=\"1440\" value=\"15\">\n            </div>\n            <div>\n              <label>Normal acknowledgement (minutes)</label>\n              <input id=\"phase19NormalAck\" type=\"number\" min=\"5\" max=\"1440\" value=\"30\">\n            </div>\n            <div>\n              <label>Urgent escalation (minutes)</label>\n              <input id=\"phase19UrgentEscalation\" type=\"number\" min=\"5\" max=\"1440\" value=\"30\">\n            </div>\n            <div>\n              <label>Normal escalation (minutes)</label>\n              <input id=\"phase19NormalEscalation\" type=\"number\" min=\"5\" max=\"1440\" value=\"120\">\n            </div>\n          </div>\n          <div class=\"row\">\n            <label>\n              <input id=\"phase19MorningEnabled\" type=\"checkbox\" style=\"width:auto\" checked>\n              Send morning briefing\n            </label>\n            <label>\n              <input id=\"phase19EodEnabled\" type=\"checkbox\" style=\"width:auto\" checked>\n              Send end-of-day briefing\n            </label>\n          </div>\n          <button type=\"submit\">Save Accountability Settings</button>\n        </form>\n        <div id=\"phase19ScheduleNote\" class=\"small muted\" style=\"margin-top:8px\"></div>\n      </div>\n\n      <div class=\"card\">\n        <h2>Recent Accountability History</h2>\n        <div id=\"phase19History\" class=\"phase19-history\">\n          <div class=\"muted\">No accountability activity yet.</div>\n        </div>\n      </div>\n    </div>\n  </div>\n</section>";

  panel = panel.replace(
    activityAnchor,
    section + "\n\n" + activityAnchor
  );

  const script = "<script>\n/* JOSHUA_PHASE19_ACCOUNTABILITY_DASHBOARD_SCRIPT_V1 */\nlet phase19Cache = null;\n\nfunction phase19Safe(value) {\n  return typeof esc === \"function\"\n    ? esc(value)\n    : String(value ?? \"\");\n}\n\nfunction phase19DateTime(value) {\n  return typeof fmt === \"function\"\n    ? fmt(value)\n    : value\n      ? new Date(value).toLocaleString()\n      : \"—\";\n}\n\nfunction phase19StatusLabel(value = \"\") {\n  return String(value || \"open\")\n    .replaceAll(\"_\", \" \");\n}\n\nfunction phase19SetMessage(text, warning = false) {\n  const box = document.getElementById(\"phase19Message\");\n  if (!box) return;\n  box.textContent = text || \"\";\n  box.className = warning\n    ? \"small warnText\"\n    : \"small muted\";\n}\n\nfunction phase19TaskMarkup(task) {\n  const status = String(\n    task.accountabilityStatus ||\n    \"awaiting_acknowledgement\"\n  );\n  const classes = [\n    \"phase19-task\",\n    status === \"escalated\" ? \"escalated\" : \"\",\n    task.overdue ? \"overdue\" : \"\"\n  ].filter(Boolean).join(\" \");\n\n  const acknowledgeButton =\n    status === \"awaiting_acknowledgement\"\n      ? `<button type=\"button\" data-phase19-ack=\"${phase19Safe(task.id)}\">Acknowledge</button>`\n      : \"\";\n\n  const workOrderButton = task.trackingNumber\n    ? `<button type=\"button\" class=\"secondary\" data-phase19-workorder=\"${phase19Safe(task.trackingNumber)}\">Open Work Order</button>`\n    : \"\";\n\n  return `\n    <div class=\"${classes}\">\n      <div class=\"phase19-task-head\">\n        <div>\n          <strong>${task.priority === \"urgent\" ? \"🚨 \" : \"\"}${phase19Safe(task.title)}</strong>\n          <div class=\"small muted\">\n            Owner: ${phase19Safe(task.assignedTo || \"Unassigned\")}\n            ${task.trackingNumber ? ` · Tracking #${phase19Safe(task.trackingNumber)}` : \"\"}\n          </div>\n          <div class=\"small\">\n            Due: ${phase19DateTime(task.dueAt)}\n            · Acknowledge by: ${phase19DateTime(task.acknowledgeBy)}\n          </div>\n        </div>\n        <span class=\"phase19-status ${phase19Safe(status)}\">${phase19Safe(phase19StatusLabel(status))}</span>\n      </div>\n      <div class=\"phase19-task-actions\">\n        ${acknowledgeButton}\n        <button type=\"button\" class=\"secondary\" data-phase19-complete=\"${phase19Safe(task.id)}\">Mark Complete</button>\n        ${workOrderButton}\n      </div>\n    </div>\n  `;\n}\n\nfunction phase19Render(data) {\n  phase19Cache = data;\n\n  const metrics = data.metrics || {};\n  document.getElementById(\"phase19Awaiting\").textContent =\n    metrics.awaitingAcknowledgement || 0;\n  document.getElementById(\"phase19Acknowledged\").textContent =\n    metrics.acknowledged || 0;\n  document.getElementById(\"phase19Escalated\").textContent =\n    metrics.escalated || 0;\n  document.getElementById(\"phase19Overdue\").textContent =\n    metrics.overdue || 0;\n  document.getElementById(\"phase19CompletedToday\").textContent =\n    metrics.completedToday || 0;\n\n  const storage = document.getElementById(\"phase19StorageStatus\");\n  if (storage) {\n    storage.textContent = data.persistentStorageActive\n      ? `Persistent storage active: ${data.persistentDataFile}`\n      : `WARNING: temporary storage is active: ${data.persistentDataFile}`;\n    storage.className = data.persistentStorageActive\n      ? \"small phase19-storage-ok\"\n      : \"small phase19-storage-warning\";\n  }\n\n  const list = document.getElementById(\"phase19TaskList\");\n  if (list) {\n    list.innerHTML = (data.tasks || []).length\n      ? data.tasks.map(phase19TaskMarkup).join(\"\")\n      : \"<div class='live'>No open accountability tasks.</div>\";\n  }\n\n  const history = document.getElementById(\"phase19History\");\n  if (history) {\n    history.innerHTML = (data.recentHistory || []).length\n      ? data.recentHistory.map(item => `\n          <div class=\"phase19-history-row\">\n            <strong>${phase19Safe(phase19StatusLabel(item.type))}</strong>\n            <div class=\"small\">${phase19Safe(item.message || \"\")}</div>\n            <div class=\"small muted\">${phase19DateTime(item.createdAt)}</div>\n          </div>\n        `).join(\"\")\n      : \"<div class='muted'>No accountability activity yet.</div>\";\n  }\n\n  const settings = data.settings || {};\n  const values = {\n    phase19MorningTime: settings.morningBriefingTime || \"07:30\",\n    phase19EodTime: settings.endOfDayBriefingTime || \"17:30\",\n    phase19UrgentAck: settings.urgentAcknowledgeMinutes || 15,\n    phase19NormalAck: settings.normalAcknowledgeMinutes || 30,\n    phase19UrgentEscalation: settings.urgentEscalationMinutes || 30,\n    phase19NormalEscalation: settings.normalEscalationMinutes || 120\n  };\n\n  for (const [id, value] of Object.entries(values)) {\n    const element = document.getElementById(id);\n    if (element) element.value = value;\n  }\n\n  const morningEnabled =\n    document.getElementById(\"phase19MorningEnabled\");\n  const eodEnabled =\n    document.getElementById(\"phase19EodEnabled\");\n\n  if (morningEnabled) {\n    morningEnabled.checked =\n      settings.morningBriefingEnabled !== false;\n  }\n  if (eodEnabled) {\n    eodEnabled.checked =\n      settings.endOfDayBriefingEnabled !== false;\n  }\n\n  const scheduleNote =\n    document.getElementById(\"phase19ScheduleNote\");\n\n  if (scheduleNote) {\n    scheduleNote.textContent =\n      `Automatic briefings begin ${data.autoBriefingsBeginDate}. ` +\n      `Last accountability sweep: ${phase19DateTime(data.lastSweepAt)}.`;\n  }\n}\n\nasync function phase19Refresh() {\n  try {\n    const data = await api(\n      \"/api/control/accountability/status\"\n    );\n    phase19Render(data);\n  } catch (error) {\n    phase19SetMessage(\n      `Accountability data unavailable: ${error.message}`,\n      true\n    );\n  }\n}\n\nasync function phase19Acknowledge(id) {\n  phase19SetMessage(\"Acknowledging task…\");\n  try {\n    await api(\n      `/api/control/tasks/${encodeURIComponent(id)}/acknowledge`,\n      {\n        method: \"POST\",\n        body: JSON.stringify({\n          actor: \"Office\"\n        })\n      }\n    );\n    phase19SetMessage(\"✅ Task acknowledged.\");\n    await phase19Refresh();\n    if (typeof refresh === \"function\") refresh();\n  } catch (error) {\n    phase19SetMessage(error.message, true);\n  }\n}\n\nasync function phase19Complete(id) {\n  phase19SetMessage(\"Completing task…\");\n  try {\n    await api(\n      `/api/control/tasks/${encodeURIComponent(id)}/complete`,\n      {\n        method: \"POST\",\n        body: JSON.stringify({\n          actor: \"Office\"\n        })\n      }\n    );\n    phase19SetMessage(\"✅ Task completed.\");\n    await phase19Refresh();\n    if (typeof refresh === \"function\") refresh();\n  } catch (error) {\n    phase19SetMessage(error.message, true);\n  }\n}\n\nasync function phase19RunNow() {\n  phase19SetMessage(\n    \"Running accountability checks and escalation rules…\"\n  );\n  try {\n    const result = await api(\n      \"/api/control/accountability/run\",\n      {\n        method: \"POST\",\n        body: \"{}\"\n      }\n    );\n    phase19SetMessage(\n      `✅ Sweep complete. ${result.notificationsSent || 0} notification(s) sent.`\n    );\n    phase19Render(result.status);\n    if (typeof refresh === \"function\") refresh();\n  } catch (error) {\n    phase19SetMessage(error.message, true);\n  }\n}\n\nasync function phase19SendBriefing(type) {\n  phase19SetMessage(\n    type === \"eod\"\n      ? \"Preparing end-of-day briefing…\"\n      : \"Preparing morning briefing…\"\n  );\n\n  try {\n    const result = await api(\n      \"/api/control/accountability/briefing\",\n      {\n        method: \"POST\",\n        body: JSON.stringify({ type })\n      }\n    );\n\n    phase19SetMessage(\n      `✅ ${result.briefing?.heading || \"Briefing\"} sent to Travis.`\n    );\n    await phase19Refresh();\n  } catch (error) {\n    phase19SetMessage(error.message, true);\n  }\n}\n\ndocument.addEventListener(\"click\", event => {\n  const acknowledge = event.target.closest?.(\n    \"[data-phase19-ack]\"\n  );\n  if (acknowledge) {\n    phase19Acknowledge(\n      acknowledge.dataset.phase19Ack\n    );\n    return;\n  }\n\n  const complete = event.target.closest?.(\n    \"[data-phase19-complete]\"\n  );\n  if (complete) {\n    phase19Complete(\n      complete.dataset.phase19Complete\n    );\n    return;\n  }\n\n  const workOrder = event.target.closest?.(\n    \"[data-phase19-workorder]\"\n  );\n  if (workOrder) {\n    const tracking =\n      workOrder.dataset.phase19Workorder;\n\n    if (\n      typeof window.openPhase12WorkOrder ===\n      \"function\"\n    ) {\n      window.openPhase12WorkOrder(tracking);\n    } else {\n      document\n        .querySelector(\n          '.tab[data-tab=\"workorders\"]'\n        )\n        ?.click();\n    }\n  }\n});\n\ndocument\n  .getElementById(\"phase19RunNow\")\n  ?.addEventListener(\"click\", phase19RunNow);\n\ndocument\n  .getElementById(\"phase19MorningBriefing\")\n  ?.addEventListener(\n    \"click\",\n    () => phase19SendBriefing(\"morning\")\n  );\n\ndocument\n  .getElementById(\"phase19EodBriefing\")\n  ?.addEventListener(\n    \"click\",\n    () => phase19SendBriefing(\"eod\")\n  );\n\ndocument\n  .getElementById(\"phase19SettingsForm\")\n  ?.addEventListener(\"submit\", async event => {\n    event.preventDefault();\n    phase19SetMessage(\n      \"Saving accountability settings…\"\n    );\n\n    const payload = {\n      morningBriefingEnabled:\n        document.getElementById(\n          \"phase19MorningEnabled\"\n        ).checked,\n      endOfDayBriefingEnabled:\n        document.getElementById(\n          \"phase19EodEnabled\"\n        ).checked,\n      morningBriefingTime:\n        document.getElementById(\n          \"phase19MorningTime\"\n        ).value,\n      endOfDayBriefingTime:\n        document.getElementById(\n          \"phase19EodTime\"\n        ).value,\n      urgentAcknowledgeMinutes:\n        document.getElementById(\n          \"phase19UrgentAck\"\n        ).value,\n      normalAcknowledgeMinutes:\n        document.getElementById(\n          \"phase19NormalAck\"\n        ).value,\n      urgentEscalationMinutes:\n        document.getElementById(\n          \"phase19UrgentEscalation\"\n        ).value,\n      normalEscalationMinutes:\n        document.getElementById(\n          \"phase19NormalEscalation\"\n        ).value\n    };\n\n    try {\n      await api(\n        \"/api/control/accountability/settings\",\n        {\n          method: \"POST\",\n          body: JSON.stringify(payload)\n        }\n      );\n      phase19SetMessage(\n        \"✅ Accountability settings saved.\"\n      );\n      await phase19Refresh();\n    } catch (error) {\n      phase19SetMessage(error.message, true);\n    }\n  });\n\nphase19Refresh();\nsetInterval(phase19Refresh, 15000);\n</script>";

  if (!panel.includes("</body>")) {
    throw new Error(
      "Could not locate the dashboard body ending for Phase 19."
    );
  }

  panel = panel.replace(
    "</body>",
    script + "\n</body>"
  );

  fs.writeFileSync(panelPath, panel);
  console.log(
    "Joshua Phase 19 accountability dashboard installed."
  );
}


/*
 * JOSHUA_PHASE19_OFFICE_SUITE_NAV_REPAIR_V1
 * Phase 10 creates the final desktop sidebar after Phase 19 adds its panel.
 * Patch the Phase 10 sidebar template before Phase 10 executes so the
 * Accountability panel remains reachable in the Office Suite navigation.
 */
const phase10Path = new URL("./phase10-bootstrap.mjs", import.meta.url);

if (fs.existsSync(phase10Path)) {
  let phase10 = fs.readFileSync(phase10Path, "utf8");
  const officeNavMarker =
    "JOSHUA_PHASE19_OFFICE_SUITE_NAV_V1";

  if (!phase10.includes(officeNavMarker)) {
    const tasksNavAnchor =
      '  <button class="office-nav-btn" data-office-tab="tasks">✓ <span>Tasks</span></button>';

    if (!phase10.includes(tasksNavAnchor)) {
      throw new Error(
        "Could not locate the Office Suite Tasks navigation button for the Phase 19 Accountability repair."
      );
    }

    const accountabilityNav =
      tasksNavAnchor +
      '\n  <!-- JOSHUA_PHASE19_OFFICE_SUITE_NAV_V1 -->' +
      '\n  <button class="office-nav-btn" data-office-tab="accountability">☑ <span>Accountability</span></button>';

    phase10 = phase10.replace(
      tasksNavAnchor,
      accountabilityNav
    );

    fs.writeFileSync(phase10Path, phase10);

    console.log(
      "Joshua Phase 19 Accountability link added to the Office Suite sidebar."
    );
  }
}

await import("./phase20-auth-bootstrap.mjs");
